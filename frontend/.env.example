VITE_API_BASE_URL=http://localhost:8000

# Propriedade do Google Analytics. Deixe VAZIO no desenvolvimento: com valor,
# a navegação local passa a contar na propriedade de produção. O valor real é
# injetado pelo deploy (.github/workflows/deploy-frontend.yml).
VITE_GA_MEASUREMENT_ID=
