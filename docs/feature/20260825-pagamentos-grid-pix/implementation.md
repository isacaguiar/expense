# Implementation — Grid de Pagamentos com Pix (QR Code + Copia e Cola)

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260825

---

## 1. Desvios do fluxo padrão

Igual ao precedente de `docs/feature/20260822-criacao-tela-pagamentos/implementation.md`: todas as tasks implementadas direto na branch da feature (`frontend/20260825-pagamentos-grid-pix`), sem sub-branch por task.

**Decisão de plan.md §7 (ExpenseView.tsx), tomada na execução da TASK-007**: `GET /api/expenses/{id}` (`ExpenseController::show`) já faz `$expense->load(['payers', 'quotas'])` e devolve o model direto; o model `Quota` tem `payment_proof_url` em `$appends`. Ou seja, `show()` **já serializa** `quotas[].payment_proof_url` sem nenhuma mudança de backend. `ExpenseView.tsx` passou a ler isso via um helper `latestPaidProof(quotas)` (mostra o comprovante da quota paga com `date_expected` mais recente, já que a tela não tem seletor de competência) — nenhuma alteração em `ExpenseController::show`.

**Estado em 2026-08-26**: linhas abaixo de TASK-004 a TASK-007 preenchidas a partir da árvore de trabalho local (ainda **não commitada** — só TASK-001..003 estão no commit `3c040e4ab`).

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-001 | Concluída | 20260825 | IA | Criado `frontend/src/components/PixPaymentDialog.tsx`. | Nenhuma mudança de backend — `/pix/generate` reaproveitado como está (specify §2.2). |
| TASK-002 | Concluída | 20260825 | IA | Criado `frontend/src/components/PayableSettlementList.tsx`. `git status` confirma `components/SettlementList.tsx` sem diff (não tocado). | — |
| TASK-003 | Concluída | 20260825 | IA | `Payments.tsx` reescrito com grid (`Grid size={{xs:12,md:7}}` despesas / `{xs:12,md:5}}` valores a pagar), busca de `/groups/{id}/members`, `DespesasThemeScope`. `Payments.test.tsx` atualizado (mock de `/members` no helper existente + 2 testes novos: abre diálogo Pix quando credor tem chave, mostra aviso quando não tem). `cd frontend && npx tsc --noEmit` → sem erro. `npx vitest run src/pages/Payments.test.tsx` → **6/6 verde** (4 testes existentes + 2 novos). Detector mecânico do impeccable (`detect.mjs --json` nos 3 arquivos tocados/criados) → `[]`, nenhum achado. | Layout desenhado com a skill `impeccable` (pedido explícito do usuário) — reaproveitou o design system escopado (`DespesasThemeScope`) já existente de `docs/feature/20260825-redesign-visual-despesas/`, sem criar tema novo. Os 2 testes de Pix foram migrados na TASK-006 para a API nova do `PayableSettlementList` (clicam no botão "Pagar com Pix" em vez do card). |
| TASK-004 | Concluída (não commitada) | 20260826 | IA | Criados `backend/database/migrations/2026_08_25_220000_create_ex_settlement_confirmations_table.php` (tabela aditiva `ex_settlement_confirmations`: `group_id`/`from_user_id`/`to_user_id` FK, `cycle_start`/`cycle_end` date, `amount` decimal(10,2), `proof_path` string obrigatório, `confirmed_at` timestamp, `unique(group_id, cycle_start, from_user_id, to_user_id)`) e `backend/app/Models/SettlementConfirmation.php` (`$fillable` dos campos, `$appends=['proof_url']` com accessor `Storage::disk('public')->url($this->proof_path)`, casts de data/`amount`). `php artisan migrate` → migration aplicada local; `php artisan migrate:status` confirma `2026_08_25_220000_create_ex_settlement_confirmations_table ... [15] Ran`. | Migration puramente aditiva (tabela nova) → autônoma no local; rodar em ambiente compartilhado/produção segue gate humano de deploy. |
| TASK-005 | Concluída (não commitada) | 20260826 | IA | `ExpenseController::confirmSettlement($request, $groupId)` novo: `authorizeGroupMembership`, valida `to_user_id` (`exists:ex_users,id`) + `comprovante` (`required|image|max:5120`), `rejectIfCompetenceClosed`, roda `computeCycleSummary` e exige um settlement `from_user_id === auth()->id()` + `to_user_id` batendo (senão 422), grava arquivo em `comprovantes-settlements/` e `SettlementConfirmation::updateOrCreate` chaveado por (`group_id`,`cycle_start`,`from_user_id`,`to_user_id`). Helper privado `attachSettlementConfirmations($groupId, Carbon $start, array $settlements)` decora cada settlement com `confirmedProofUrl`/`confirmedAt`, chamado nos 3 `return` de `summary()`/`close()`/`reopen()`. Rota `POST /groups/{groupId}/settlements/confirm` dentro de `jwt.auth`. Novo `backend/tests/Feature/SettlementConfirmationControllerTest.php` (8 casos: confirma settlement real → 200 + `amount` líquido + `proof_url`; `comprovante` obrigatório → 422; sem settlement real pro par → 422 + 0 registros; reenvio troca o `proof_path` mantendo 1 registro; ciclo fechado manualmente → 422; não-membro → 404; `summary` expõe `confirmedProofUrl`/`confirmedAt` no settlement certo; `summary` devolve `null` quando ninguém confirmou). `php artisan test --filter=SettlementConfirmationControllerTest` → 8/8 (24 assertions). `php artisan test` (suíte completa) → **212/212 (650 assertions)**, sem regressão. | Desvio do critério de aceite: dizia "membro não autenticado recebe 401" — a checagem real é `authorizeGroupMembership`, que devolve **404** pra não-membro (mesmo padrão das outras rotas do controller); requisição sem token continua 401 pelo middleware. Coberto também `rejectIfCompetenceClosed` (não estava no critério). Critério de aceite ajustado no `tasks.md`. |
| TASK-006 | Concluída (não commitada) | 20260826 | IA | `PayableSettlementList.tsx`: props trocadas de `onSelect(settlement)` (card inteiro como `CardActionArea`) para `currentUserId` + `onPayWithPix(settlement)` + `onSendProof(settlement)`; card vira `Card` normal com dois botões ("Pagar com Pix" / "Enviar comprovante"·"Reenviar comprovante") visíveis só quando `currentUserId === settlement.from_user_id`, e um `Chip` "Comprovante enviado" (link pro `confirmedProofUrl`) visível pra qualquer membro quando há confirmação. `Payments.tsx`: estado do diálogo de upload (`confirmSettlementTarget`/`selectedSettlementFile`/…), `POST /groups/{id}/settlements/confirm` multipart (`to_user_id` + `comprovante`), `Snackbar` de sucesso, `reload()` no sucesso. `SummarySettlement` (`useGroupCycle.ts`) ganhou `confirmedProofUrl`/`confirmedAt`. `Payments.test.tsx`: os 2 testes de Pix da TASK-003 migrados (clicam no botão `"Pagar com Pix"` em vez do card) + 1 teste novo ("sends the debtor proof as multipart to /settlements/confirm and shows a success message" — abre "Enviar comprovante", "Confirmar" desabilitado até anexar arquivo, `POST` multipart com `to_user_id`+`comprovante`, snackbar "Comprovante enviado."). `npx tsc --noEmit` → sem erro. `npx vitest run src/pages/Payments.test.tsx` → **7/7 verde**. | Suíte completa do frontend (`npx vitest run`) teve 1 flake pré-existente sem relação (`GroupForm.test.tsx > sends closing_day in the payload when filled`, verde 3/3 rodando isolado) — só aparece sob contenção do run paralelo cheio, não é regressão desta task. |
| TASK-007 | Concluída (não commitada) | 20260826 | IA | `ExpenseManager.tsx`: `IconButton` com `ImageOutlinedIcon` ("Ver comprovante", `component="a"` `target="_blank"`) na linha da tabela quando `exp.paid && exp.paymentProofUrl`, e `MuiLink` "Ver comprovante" no modal de detalhe (`detailExpense.paid && detailExpense.paymentProofUrl`). `ExpenseView.tsx`: tipo `ExpenseQuota` + helper `latestPaidProof(quotas)` lendo `expense.quotas[].payment_proof_url` (sem mudança de backend — ver §1), `MuiLink` "Ver comprovante" abaixo do credor quando há `proofUrl`. `ExpenseManager.test.tsx`/`ExpenseView.test.tsx` atualizados. `npx tsc --noEmit` → sem erro. `npx vitest run src/pages/ExpenseManager.test.tsx src/pages/ExpenseView.test.tsx` → verde (os 2 arquivos passam; as 2 falhas do run conjunto de 3 arquivos são todas de `Payments.test.tsx`/TASK-006). | Independente da TASK-005/006 (specify §2.8 / plan.md §N) — pode fechar sem elas. |
