# Implementation — Data de atualização por documento legal

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260920

---

## 1. Desvios do fluxo padrão (se houver)

Sem desvio de fluxo. Uma particularidade de verificação:

- **O site não tem suíte automatizada**, então as tasks são verificadas por `php -l`, execução real (`php -r` para o helper, servidor `site-static` em `localhost:4173` para as páginas) e, nas extrações de conteúdo, por `diff` do HTML renderizado antes e depois.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-321 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l site/src/helpers.php` → "No syntax errors detected". **(1) Fallback**: com `legal-dates.php` ausente, `legal_updated_at('privacidade')` → `20 de setembro de 2026` e `('termos')` → `20 de setembro de 2026`, com o relógio local em `2026-09-20 22:59`. **(2) Precedência**: com um `legal-dates.php` de teste (`privacidade => 2026-03-05`, `termos => 2025-12-31`), as chamadas devolvem `5 de março de 2026` e `31 de dezembro de 2025` — o artefato ganha do `filemtime`. **(3) Sem `intl`**: `php -m \| grep -c -i '^intl$'` → `0`, e nada falha. **(4) Documento inexistente** devolve `""` em vez de erro. | **Bug pego na verificação, corrigido dentro da task**: a primeira versão usava `date('Y-m-d', filemtime(...))` e devolveu `21 de setembro` com o relógio local em `20/09 22:58`. Causa: `ini_get('date.timezone')` está **vazio** neste projeto, então o PHP assume UTC — às 22h locais já é o dia seguinte em UTC. Passou a converter com `DateTimeImmutable` + `DateTimeZone(FUSO_DO_PROJETO)`, cumprindo o specify §2.4. A constante `FUSO_DO_PROJETO` foi criada no mesmo arquivo para o gerador da TASK-325 usar o mesmo valor. O fallback aceita o arquivo de conteúdo **ou** a própria página, porque `site/src/legal/` só passa a existir na TASK-323 — sem isso esta task não seria verificável sozinha. |
| TASK-322 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l` limpo nas duas páginas. `grep -c "config['updated_at']" site/public/privacidade.php site/public/termos.php` → `0` nas duas. Renderizado em `localhost:4173`: `/privacidade.php` e `/termos.php` exibem "Última atualização: 20 de setembro de 2026", e a contagem de `Warning:|Notice:|Fatal|Undefined` no HTML é `0` em ambas. | **Critério da task corrigido durante a execução** (registrado em `tasks.md`): a redação original pedia `grep -c "updated_at"` igual a `0`, impossível de cumprir porque o nome do próprio helper — `legal_updated_at` — contém a string. O que importa é não restar leitura de `$config['updated_at']`. A mesma correção foi aplicada ao critério da TASK-327, que tinha o defeito idêntico. As duas páginas ainda mostram a mesma data porque ambas caem no `filemtime` das próprias páginas — a diferenciação real chega com a extração do conteúdo (TASK-323/324) e com o artefato do deploy (TASK-325/326). |
| TASK-323 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l` limpo em `site/src/legal/privacidade.php` e na página. HTML renderizado capturado antes e depois: `diff` ignorando CR → **idêntico**, nenhum caractere de texto perdido; `<h2>` contados → `8` antes e `8` depois; `grep -ciE "Warning:|Notice:|Fatal|Undefined"` no HTML → `0`. A data passou a sair do arquivo de conteúdo: `legal_updated_at('privacidade')` → `20 de setembro de 2026`. | **Duas tentativas.** A primeira extração perdeu a linha em branco após `</header>` e indentou a primeira `<section>`; refeita levando as linhas 20..116 e pondo o `require` na coluna 0, já que o `?>` consome só a quebra imediatamente seguinte. **Diferença residual de 104 bytes investigada até o fim**: são 104 linhas que perderam o `
| TASK-324 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l` limpo nos dois arquivos. HTML antes × depois: `diff` ignorando CR → **idêntico**; `<h2>` → `9` antes e `9` depois; erros PHP no HTML → `0`; delta de 89 bytes = exatamente os 89 CR perdidos (220 → 131), mesmo artefato local de CRLF da TASK-323. **Critério da data de autoria**: `TZ=America/Sao_Paulo git log -1 --date=format-local:'%Y-%m-%d' --format=%ad -- site/src/legal/termos.php` → **`2026-08-24`**, enquanto `%cd` (commit) → `2026-09-20`. **Simulação do estado final**, escrevendo à mão o artefato que a TASK-325 vai gerar: `/termos.php` passa a exibir "24 de agosto de 2026" e `/privacidade.php` "20 de setembro de 2026" — as duas datas finalmente diferentes e verdadeiras. Artefato removido em seguida. | A extração foi commitada com `git commit --date="2026-08-24T12:00:00-03:00"`, em commit separado do log de implementação, para que `git log -1 -- <arquivo de conteúdo>` devolva exatamente esse commit. Sem isso o arquivo nasceria hoje e a página anunciaria uma atualização do contrato que não houve — o erro que esta feature existe para evitar. A data de commit continua sendo a real: só a autoria é retroativa, que é a distinção que o git faz entre quando o texto foi escrito e quando entrou no repositório. Localmente a página ainda mostra 20/09 porque o fallback usa `filemtime` e o arquivo nasceu hoje; em produção quem manda é o artefato. |
| TASK-325 | Concluída | 2026-09-20 | Claude Opus 5 | **Execução normal**: `bash site/tools/gerar-datas-legais.sh` → gera o arquivo e imprime `privacidade -> 2026-09-20`, `termos -> 2026-08-24`; `php -r` sobre o gerado devolve os mesmos dois pares. **Falha ruidosa em clone raso**, testada de verdade: `git clone --depth 1 file://<repo>` em diretório temporário, `git rev-parse --is-shallow-repository` → `true`, e o script sai com **código 1** sem escrever arquivo nenhum. `git check-ignore -v site/src/legal-dates.php` → `.gitignore:35`. | **O guard mudou por causa do que o teste mostrou.** O plano supunha detectar o clone raso por "git log veio vazio". No clone raso real, `git log -1 --format=%ad -- site/src/legal/termos.php` devolve **`2026-09-20`** — data errada e **não vazia**, porque sem histórico o git enxerga todo arquivo como criado no commit do topo. Um check de saída vazia teria passado e publicado os Termos com a data do deploy, que é exatamente o defeito que esta feature existe para evitar. O script passou a checar `git rev-parse --is-shallow-repository` explicitamente, e manteve a checagem de vazio como segunda linha. Escreve o arquivo só depois de obter todas as datas: um arquivo parcial seria pior que nenhum, porque `legal_updated_at()` confia no que estiver lá. |
| TASK-326 | Concluída | 2026-09-21 | Claude Opus 5 | `grep -n "fetch-depth: 0" .github/workflows/deploy-site.yml` → linha `24`, dentro do `with:` do checkout. **YAML validado de fato**, com o parser `yaml` encontrado em `frontend/node_modules/cosmiconfig/node_modules/yaml`: o documento faz parse, tem 4 passos na ordem `Clonar → Gerar datas → Deploy public → Deploy src`, `steps[0].with["fetch-depth"]` é `0`, `steps[1].run` é `bash site/tools/gerar-datas-legais.sh`, e o gerador vem antes dos dois passos de FTP. | O gerador precisa rodar antes do upload de `site/src/`, porque é lá que ele escreve o artefato — a ordem não é estética. `pyyaml` e `js-yaml` não existem neste ambiente; em vez de marcar o critério como "não verificável", usei o `yaml` que veio como dependência transitiva do `cosmiconfig` no `node_modules` do frontend. O comentário no workflow registra que a ausência de `fetch-depth: 0` **quebra o deploy** (o script aborta) em vez de publicar data errada — para quem mexer no arquivo depois não remover a linha achando que é supérfluo. |
` (235 CR antes, 131 depois), porque o arquivo novo nasceu em LF e a cópia local da página está em CRLF por `core.autocrlf=true`. Não existe em produção — `git show :arquivo` mostra **0 CR** nos dois blobs, e o runner é Linux. O critério "diff vazio" vale em substância; o byte a byte local é artefato do Windows. **Uma armadilha de verificação no meio do caminho**: a captura "depois" veio vazia e o diff acusou perda total do documento. Causa: o servidor `site-static` tinha sido derrubado — não era o código. Religado antes de concluir qualquer coisa. O corte separa texto jurídico de moldura: o `doc-header` (título + data) fica na página, porque data não é texto do contrato e tê-la no arquivo de conteúdo faria a data se alterar sozinha. |
