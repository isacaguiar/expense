# Implementation — Atualização de Layout das Demais Páginas

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260820

---

## 1. Desvios do fluxo padrão

- **Branch criada a partir de `frontend/20260819-novo-layout-tela-entrada`, não de `dev`.** `04-implementation.md` §1 pede branch a partir de `dev` atualizada. Esta feature depende diretamente de código que só existe naquela branch (`GroupSummarySidebar`/`GroupSummaryHeader`, `theme/brandColors.ts`) — o PR dela contra `dev` (`https://github.com/isacaguiar/expense/pull/new/frontend/20260819-novo-layout-tela-entrada`) ainda não foi aprovado/mergeado (confirmado via `git merge-base --is-ancestor ... origin/dev` → não é ancestral). Branchear de `dev` significaria reescrever esses componentes do zero ou duplicá-los. Consequência: o PR desta feature contra `dev` só pode ser aberto/mergeado depois (ou junto) do PR de `novo-layout-tela-entrada` — vai carregar os commits daquela branch até lá ser integrada.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-088 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/GroupSummary.test.tsx` — 9/9 (1 falha inicial ajustada, ver observação). | `GroupSidebar`/`GroupHeader`/`getInitials` movidos de `pages/summary/` para `layouts/group/`; `GroupHeader` agora recebe `title` via prop. Aproveitei para já aplicar a decisão do `plan.md` §1 de apontar "Configurações" para `/groups/:id/edit` (estava `href="#"`) — ajustei a asserção correspondente em `GroupSummary.test.tsx` (era o único teste que quebrou). |
| TASK-089 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/GroupSummary.test.tsx src/layouts/GroupShellLayout.test.tsx` — 10/10. `npx vitest run` (suíte completa) — 30/30. | Criado `GroupShellLayout.tsx` (sidebar+header, busca `groups`/`userName` uma vez, título derivado do item ativo de `groupNavItems`, troca de grupo preserva a página atual via `location.pathname.replace`). `GroupSummary.tsx` migrado para consumir o shell via `<Outlet/>` (perdeu os `useEffect` de `groups`/`userName` e o wrapper `Box`/`Container`). Rota `/groups/:id/summary` movida para dentro de `<GroupShellLayout/>` em `App.tsx`. 3 testes que validavam comportamento de shell (link da sidebar, nome/iniciais do usuário, navegação ao trocar de grupo) saíram de `GroupSummary.test.tsx` e viraram `GroupShellLayout.test.tsx` (testado com `Routes` reais, não mais `useParams` mockado, para cobrir a derivação de título e a preservação de página ao trocar de grupo). |
| TASK-090 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/layouts/SimpleShellLayout.test.tsx` — 2/2. `npx vitest run` (suíte completa) — 32/32. | Criado `SimpleShellLayout.tsx` (cabeçalho com wordmark + avatar/nome via `GET /api/me`, `flexWrap: 'wrap'` para não quebrar em telas estreitas, sem sidebar/seletor de grupo — confirmado por teste que `role="navigation"`/`combobox` não existem nesse shell). Extraí o bloco de logo+wordmark de `GroupSidebar` (mesmo padrão visual que `LoginBrandingPanel` também usa, mas esse arquivo não foi tocado — feature de login já está em PR separado) para um componente `BrandWordmark.tsx` compartilhado (`layouts/`), usado agora por `GroupSidebar` e `SimpleShellLayout` (mesmo resultado visual em `GroupSidebar` — suíte completa confirma). |
| TASK-091 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run` — 32/32. Verificação manual no browser (`preview_start` em `http://localhost:3000`, token fake em `localStorage` só pra passar do `RequireAuth`): `/dashboard` renderiza dentro de `SimpleShellLayout` (banner+main, sem sidebar); `/groups/1/edit` renderiza dentro de `GroupShellLayout` com os 6 itens da sidebar, "Configurações" ativo apontando pra própria rota, título do header derivado corretamente ("Configurações") — sem console error de render, só os 401 esperados do token fake contra o backend. | `InternalLayout`/`Navbar` não são mais referenciados em `App.tsx` (confirmado via `grep` — só `InternalLayout.tsx` se autorreferencia); arquivos não apagados nesta feature (decisão do `plan.md` §1). **Desvio pontual do specify/plan**: percebi que `SummaryEntry.tsx` (rota `/summary`, seletor de grupo para a tela de Resumo) tinha ficado de fora da lista de páginas do `specify.md` por descuido — é o mesmo padrão de `ExpensesEntry.tsx` (R6), então incluí junto sob `SimpleShellLayout` nesta task, sem mudança de comportamento. Rota `/groups` (GroupList) também movida para `SimpleShellLayout` como passo intermediário — conteúdo dela ainda será substituído na TASK-092. |
| Correção pós-TASK-091 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run` — 33/33 (novo teste "shows a generic menu..." + "derives the header title..." em `SimpleShellLayout.test.tsx`, substituindo o teste antigo que afirmava "no sidebar"). Verificação manual no browser: token fake em `localStorage`, `navigate('/dashboard')`, `read_page` confirma `navigation` com 6 links (Meus Grupos→`/dashboard`, Resumo→`/summary`, Despesas→`/expenses` reais; Participantes/Pagamentos/Configurações com `href="#"`) e `heading "Meus Grupos"` no header — token removido ao final. | Usuário reportou (fora do fluxo de tasks): "A pagina do dashboard não apresentou o menu / Ela deve mostrar o menu como na pagina de principal de Resumo". Isso reverte a decisão original de R1/`plan.md` §1 (cabeçalho simples sem sidebar fora de um grupo) — corrigido a pedido direto do usuário, com `specify.md` R1 e `plan.md` §1/§8 atualizados para refletir a decisão nova. Extraída a renderização da sidebar de `layouts/group/GroupSidebar.tsx` para um componente genérico `layouts/Sidebar.tsx` (recebe `items: GroupNavItem[]`); `GroupSidebar` virou wrapper fino sobre ele — sem mudança de comportamento/DOM na sidebar já usada por `GroupShellLayout` (`GroupShellLayout.test.tsx` continua verde sem alteração). Nova função `layouts/simpleNavItems.ts` gera os itens genéricos (sem depender de `groupId`). `SimpleShellLayout` passou a reaproveitar o próprio `GroupHeader` (com `groups=[]`/`groupId=''`) em vez de montar um cabeçalho próprio — `GroupHeader` já escondia o seletor de grupo quando não há grupo, então não precisou de componente novo. |
| TASK-092 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run` — 37/37 (9 arquivos — `GroupList.test.tsx` removido junto com a página, `Dashboard.test.tsx` novo com 5 casos). Verificação manual: token fake em `localStorage`, `navigate('/dashboard')` — confirma o novo guard de 401 disparando corretamente (bounce pra `/`, mesmo padrão de `GroupList`/`ExpenseManager`/`GroupSummary`), token removido em seguida. | `Dashboard.tsx` virou a página "Meus Grupos" consolidada: busca client-side (preservada de `GroupList.tsx`), botão "Novo grupo" (preservado de `GroupList.tsx`, `GroupList.tsx` tinha o único botão funcional pra `/groups/new`), cards com 3 ações (editar/participantes/despesas, preservadas de ambas as páginas antigas) + clique no card/nome navega pra `/groups/:id/summary` (substitui a 4ª ação "ir pro Resumo" que só `Dashboard.tsx` antigo tinha, agora redundante), estado vazio com o texto de `ExpensesEntry.tsx:73`. **Preservado da versão antiga de `Dashboard.tsx`** (que não tinha isso): guard de 401 → `navigate('/', {replace:true})`, portado de `GroupList.tsx` — antes o `Dashboard.tsx` engolia qualquer erro (`catch(() => setGroups([]))`) e mostrava uma lista vazia mesmo com token inválido; agora segue o mesmo padrão de todas as outras páginas. `GroupList.tsx`/`GroupList.test.tsx` apagados (`git rm`); único teste de lá (redirect em 401) portado pro `Dashboard.test.tsx` novo. Rota `/groups` removida de `App.tsx`; nenhuma outra referência a `/groups` (fora do próprio `Navbar.tsx`, já morto/não referenciado) restava no código. Título "Meus Grupos XXX" com resíduo de debug (`GroupList.tsx:65`, backlog `027`) não foi reaproveitado — página nova usa o título derivado pelo shell ("Meus Grupos", sem "XXX"). |
| TASK-093 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/GroupForm.test.tsx` — 2/2 (sem alteração nos testes — mesmos labels/roles, só a casca visual mudou). Verificação manual: `/groups/new` (dentro de `SimpleShellLayout`) — `read_page` confirma `heading "Criar Novo Grupo"` + os 3 campos + botões dentro do card; `/groups/1/edit` (dentro de `GroupShellLayout`) — sidebar/header corretos, formulário em estado de loading (token fake gera 401 no `GET /api/groups/1`, comportamento pré-existente e correto — `GroupForm` só mostra erro genérico nesse caso, não redireciona). | Removido o `Container maxWidth="md"` externo (os shells já fornecem `Container` — ficaria `Container` dentro de `Container`); formulário agora é um `Card`/`CardContent` centralizado (`maxWidth: 520, mx: 'auto'`), mesmos 3 campos e mesmo `handleSubmit`/validação de hoje, sem campo novo. |
| TASK-094 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/GroupMembersForm.test.tsx` — 2/2 (arquivo novo, página não tinha teste antes). `npx vitest run` (suíte completa) — 39/39. | `<table>` HTML crua (`GroupMembersForm.tsx:104-123` na versão antiga) substituída por `List`/`ListItemAvatar` com `Avatar` de iniciais (`getInitials`/`brandColors`, mesmo padrão de "Saldos por pessoa" do `GroupSummary.tsx` — `getInitials` de um e-mail sem espaço retorna só 1 letra, já que o endpoint de membros não expõe nome, só e-mail). Duas colunas mantidas (lista \| formulário), cada uma agora dentro de um `Card`. Removido o `Container` externo (shells já fornecem um) e o `<Typography variant="h4">` de título duplicado (título "Participantes" já vem do `GroupShellLayout`, derivado de `groupNavItems`). Mesmos endpoints/`handleAddMember` de hoje — sem mudança de contrato. |
| TASK-095 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/ExpensesEntry.test.tsx src/pages/SummaryEntry.test.tsx` — 6/6 (3 casos cada, `ExpensesEntry.test.tsx` é novo — espelha os 3 casos que `SummaryEntry.test.tsx` já tinha). `npx vitest run` (suíte completa) — 42/42. Verificação manual: `/expenses` com token fake — `read_page` confirma o bounce pro login (401 esperado no `GET /api/groups`, mesmo padrão de `Dashboard`); layout dos cards em si já coberto pelos testes com dado mockado (2+ grupos). | **Desvio pontual, mesmo padrão já registrado na TASK-091**: restilizei `SummaryEntry.tsx` junto (não é literalmente `ExpensesEntry.tsx` do título da task, mas é o mesmo componente-espelho, mesma R6) — as duas páginas eram cópias quase idênticas (só o destino do card muda, `/groups/:id/expenses` vs `/groups/:id/summary`). Removido `Container` externo e o título "Escolha um grupo" (h4) de ambas — mesmo padrão de "sem título duplicado" já usado em `Dashboard.tsx`/`GroupMembersForm.tsx` (o shell já mostra "Despesas"/"Resumo" no header, derivado de `simpleNavItems`). Cards trocaram `component={Link}` direto por `CardActionArea` (mesmo padrão adotado em `Dashboard.tsx` na TASK-092). Nenhuma mudança de comportamento (redirecionamento automático com 1 grupo, escolha com 2+, mensagem vazia com 0) — `SummaryEntry.test.tsx` já existente continua verde sem alteração. |
| TASK-096 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/ExpenseManager.test.tsx src/pages/ExpenseForm.test.tsx` — 10/10. `npx vitest run` (suíte completa) — 44/44. Verificação manual: token fake, `/groups/1/expenses/new` — `read_page` confirma o card "Cadastrar nova despesa" com todos os campos (descrição, valor, tipo, data, pagador, participantes) dentro do `GroupShellLayout`, sem `Dialog`/modal. | Criado `pages/ExpenseForm.tsx` — mesma lógica de `handleSaveExpense`/`buildInstallmentQuotas`/`addMonthsClamped` que estava no modal de `ExpenseManager.tsx`, casca visual virou `Card` de página cheia; ao salvar, `navigate('/groups/:id/expenses')` (antes era só fechar o modal + recarregar a lista no mesmo componente). `ExpenseManager.tsx` perdeu todo o estado/handlers do modal (`openModal`, `newDescription` etc., fetch de `members`/`currentUserId` que só existia para o modal) — botão "Nova Despesa" agora só faz `navigate`. Também removido um `console.error` de debug com bug de template string (faltava crase, `'Load expenses for groupId: ${groupId}'` imprimia a variável literal) e o título duplicado "Despesas do Grupo"/"Grupo ID: X" do cabeçalho (shell já mostra "Despesas", mesmo padrão das tasks anteriores) — ambos no mesmo bloco de código que já estava sendo editado para o botão, não é escopo novo. Nova rota `/groups/:id/expenses/new` em `App.tsx`, dentro de `GroupShellLayout`. Os 7 testes de criação de despesa que viviam em `ExpenseManager.test.tsx` (testando o modal) foram portados para `ExpenseForm.test.tsx` novo, testando a página diretamente — mesmas asserções de payload, mais uma nova checando o `navigate` de volta pra listagem. `ExpenseManager.test.tsx` ganhou 1 teste novo (clicar "Nova Despesa" navega, não abre mais modal); o describe de "remover despesa Fixa" ficou intacto. |
| TASK-097 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/ExpenseView.test.tsx` — 3/3 (encontrada, não encontrada, 401→login). `npx vitest run` (suíte completa) — 47/47. Verificação manual: token fake, `/groups/1/expenses/9` — `read_page` confirma o bounce pro login (401 esperado no `GET /groups/1/expenses`, mesmo padrão de todas as outras páginas); estados "encontrada"/"não encontrada" já cobertos pelos testes com dado mockado. | Criado `pages/ExpenseView.tsx` — busca a despesa via `GET /groups/:id/expenses?year&month` do **mês atual** (mesmo endpoint/parâmetros que `ExpenseManager.tsx` já usa, sem endpoint `show` — trade-off já documentado em `plan.md` §4/backlog `023`) e procura o `expenseId` da URL dentro do array retornado; se não encontrar (despesa de outro mês, ou id inexistente), mostra "Despesa não encontrada" com link de volta, sem lançar erro. Sem botão "Editar" (decisão do `plan.md` §4 — sem endpoint `update`, não teria o que editar de verdade). Nova rota `/groups/:id/expenses/:expenseId` em `App.tsx`, depois de `/groups/:id/expenses/new` (React Router já prioriza segmento estático `new` sobre `:expenseId` por ranking de rota, independente da ordem de declaração — confirmado navegando pra `/groups/1/expenses/new` continuar abrindo `ExpenseForm`, não `ExpenseView`). Ainda não há link partindo da listagem de `ExpenseManager.tsx` pra essa rota — fica pra TASK-098, quando a listagem em `Table` vira cards (evita restilizar a listagem duas vezes). |
| TASK-098 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/ExpenseManager.test.tsx` — 6/6 (3 novos: cards com link pra `ExpenseView`, filtro por busca, filtro por tipo). `npx vitest run` (suíte completa) — 50/50. | `Table`/`TableRow` viraram `Grid` de `Card`s (mesmo padrão visual de `GroupSummary.tsx`/`Dashboard.tsx`): ícone por tipo (`AutorenewOutlinedIcon`/`ReceiptOutlinedIcon`, mesmo par usado em "Despesas do ciclo" do Resumo) + `Chip` "Fixa"/"Variável", valor, data e pagador. Cada card usa `CardActionArea` levando pra `/groups/:id/expenses/:id` (fecha o link que faltava da TASK-097) — `IconButton` de excluir despesa Fixa ficou **fora** da `CardActionArea`, num `CardActions` separado, pra não aninhar elemento interativo dentro de outro (mesmo cuidado já tomado em `Dashboard.tsx`). Busca (`TextField`) e filtro (`ToggleButtonGroup` Todas/Fixas/Variáveis) são só `Array.filter` sobre `expenses` já carregado do mês — sem chamada nova à API (teste confirma `axios.get` chamado só 1x ao digitar na busca). Sem paginação real (fora de escopo, backlog `026`) — lista o mês inteiro. |
| TASK-099 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run src/pages/ExpenseManager.test.tsx` — 7/7 (1 novo: descrição da despesa no diálogo + toast de sucesso). `npx vitest run` (suíte completa) — 51/51. | `Dialog` de confirmação ganhou `PaperProps={{sx:{borderRadius:2}}}` (mesmo raio dos cards do resto do app) e passou a citar a descrição da despesa (`removeExpense = expenses.find(...)`) em vez do texto genérico "esta despesa" — mesmas 2 opções de hoje ("a partir deste mês"/"a partir do mês que vem"), mesmo `stopFixedRecurrence`/endpoint. Adicionado `Snackbar`+`Alert` (`severity="success"`, `autoHideDuration=4000`) disparado no `.then()` de sucesso — não muda o fluxo de erro (`alert()` de falha continua igual, fora do escopo desta task). |
| TASK-100 | Concluída | 2026-08-20 | IA (Claude) | Verificação manual: `resize_window` não altera de fato o viewport nesse ambiente (limitação já conhecida — `innerWidth` continuou 981 mesmo após `preset: "mobile"` + reload), então inspecionei a regra CSS gerada diretamente via `javascript_tool`: `document.styleSheets` confirma `@media (min-width: 0px) { .css-xxx { display: none } }` + `@media (min-width: 900px) { .css-xxx { display: flex } }` na classe do `<nav>` do `Sidebar.tsx` — exatamente o breakpoint `md` do MUI (900px), oculta abaixo dele. Cabeçalho (`GroupHeader`, reaproveitado pelos dois shells): `getComputedStyle` confirma `flex-wrap: wrap`; `document.documentElement.scrollWidth` (981) ≈ `clientWidth` (980), sem overflow horizontal. | Nenhuma mudança de código — só verificação. O mecanismo de ocultar a sidebar é herdado sem alteração do `GroupSummarySidebar` original (já testado/comitado na feature anterior, `TASK-086`); a extração pra `Sidebar.tsx` genérico (correção pós-TASK-091) manteve o mesmo `sx={{ display: { xs: 'none', md: 'flex' } }}`, então o comportamento se aplica igualmente a `GroupShellLayout` e `SimpleShellLayout` agora que os dois usam o mesmo componente. |
| TASK-101 | Concluída | 2026-08-20 | IA (Claude) | `npx tsc --noEmit` — limpo. `npx vitest run` — **51/51** em 13 arquivos. `npx vite build` — build de produção limpo (warning de chunk >500kB é pré-existente, não relacionado a esta feature). | Consolidação final — os testes já foram escritos/ajustados incrementalmente a cada task (convenção seguida durante toda a feature, não só nesta task final), então TASK-101 é o passo de conferência, não de escrita em massa. Checklist do critério de aceite: (1) `GroupSummary.test.tsx` — verde desde a TASK-089, sem regressão; (2) `Dashboard.test.tsx` — novo, 5 casos, **`GroupList.test.tsx` conferido antes de apagar (TASK-092) e seu único caso (redirect em 401) preservado** dentro do arquivo novo, conforme pedia este critério; (3) `GroupForm.test.tsx` — pré-existente, 2 casos, sobrevive sem alteração à TASK-093 (só casca visual mudou); (4) `GroupMembersForm.test.tsx` — novo (página não tinha teste), 2 casos; (5) `ExpensesEntry.test.tsx` — novo espelhando `SummaryEntry.test.tsx`, 3 casos cada; (6) `ExpenseForm.test.tsx` — novo, 7 casos (criar via página, incluindo os 6 portados do modal antigo); (7) `ExpenseView.test.tsx` — novo, 3 casos (encontrada / não encontrada / 401); (8) `ExpenseManager.test.tsx` — 7 casos (navegação pra criação, listagem em cards + busca + filtro, exclusão de Fixa com diálogo+toast). Nenhum arquivo de teste foi removido sem seus casos migrados. Build de produção rodado como conferência extra (não pedido explicitamente no critério de aceite, mas relevante dado o volume de arquivos criados/apagados/movidos nesta feature) — `dist/` gerado localmente foi revertido via `git checkout` antes de commitar (artefato de build está versionado no repo por decisão pré-existente, não relacionada a esta feature; não fazia sentido incluir uma rebuild local no commit). |
