# Implementation — Melhoria em Grupo, Conta e Participantes

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260822

---

## 1. Desvios do fluxo padrão (se houver)

<Deixe vazio/apague esta seção se a feature segue `04-implementation.md` sem exceção.>

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-181 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `abort_if($group->deleted, 404)` adicionado em `GroupMemberController@store` (`backend/app/Http/Controllers/GroupMemberController.php`) logo após `authorizeMembership`; teste novo `test_cannot_add_member_to_deleted_group` em `backend/tests/Feature/GroupMemberControllerTest.php`. `cd backend && ./vendor/bin/pint --test` — 9 issues pré-existentes em arquivos não tocados por esta task (`PixPayload.php`, `UserInvitedMail.php`, `Expense.php`, `User.php`, migrations antigas), nenhum nos arquivos alterados. `php artisan test --filter=GroupMemberControllerTest` — 3 passed (10 assertions). `php artisan test` (suíte completa) — 170 passed (488 assertions). | Branch `backend/20260822-melhoria-grupo-conta-participantes` criada a partir de `dev` (primeira task da feature, implementada direto nela). Commit `1358c4f9e`. |
| TASK-182 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `abort_if($group->deleted, 404)` adicionado em `ExpenseController@store` (após `authorizeGroupMembership`, antes da validação de payload), `@close` e `@reopen` (`backend/app/Http/Controllers/ExpenseController.php`); testes novos `test_cannot_create_expense_in_deleted_group` (`ExpenseControllerStoreTest.php`), `test_cannot_close_deleted_group` (`ExpenseControllerCloseTest.php`), `test_cannot_reopen_deleted_group` (`ExpenseControllerReopenTest.php`). `./vendor/bin/pint --test` nos 4 arquivos tocados — PASS, sem issues. `php artisan test --filter="ExpenseControllerStoreTest\|ExpenseControllerCloseTest\|ExpenseControllerReopenTest"` — 33 passed (107 assertions). `php artisan test` (suíte completa) — 173 passed (495 assertions). | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-182` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `614e158bb`, merge sem PR conforme `ADR-003`. |
| TASK-183 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | Relação `Group::cycleSnapshots()` (`hasMany(GroupCycleSnapshot::class, 'group_id')`) adicionada em `backend/app/Models/Group.php`. `GroupController@destroy` (`backend/app/Http/Controllers/GroupController.php`) ramifica em `$group->cycleSnapshots()->exists()`: com competência fechada mantém `deleted=true`; sem competência fechada, `DB::transaction` apagando `participations()`, depois por despesa `quotas()`, `payers()->detach()` e a despesa, depois `members()->detach()` e o grupo. Confirmado que `ex_expenses.group_id` **não** tem `ON DELETE CASCADE` (`database/migrations/2025_06_12_013849_create_ex_expenses_table.php:30`) — por isso as despesas precisam ser apagadas explicitamente antes do grupo, a cascata do app não é redundante. Testes: `test_member_can_delete_group_without_closed_cycle_physically` e `test_member_can_delete_group_with_closed_cycle_logically_preserving_history` substituem o antigo `test_member_can_delete_group` (que assumia soft delete sempre) em `backend/tests/Feature/GroupControllerTest.php`. `./vendor/bin/pint --test` nos 3 arquivos tocados — PASS. `php artisan test --filter=GroupControllerTest` — 19 passed (59 assertions). `php artisan test` (suíte completa) — 174 passed (508 assertions). | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-183` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `950d8c5d2`. |
| TASK-184 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `->withExists('cycleSnapshots')` adicionado à query de `GroupController@index` (`backend/app/Http/Controllers/GroupController.php`), ao lado do `withMax('expenses', 'date_payment')` já existente — popula `cycle_snapshots_exists` (boolean) em cada grupo do JSON de `GET /api/groups`. Testes novos `test_index_includes_cycle_snapshots_exists_true_when_group_has_closed_cycle` e `test_index_includes_cycle_snapshots_exists_false_without_closed_cycle` em `backend/tests/Feature/GroupControllerTest.php`. `./vendor/bin/pint --test` nos 2 arquivos tocados — PASS. `php artisan test --filter=GroupControllerTest` — 21 passed (65 assertions). `php artisan test` (suíte completa) — 176 passed (514 assertions). | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-184` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `23480ab4f`. Fecha a parte de backend do plan.md §2 — falta só a TASK-185 (frontend) consumir esse campo. |
| TASK-185 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `frontend/src/pages/Dashboard.tsx`: tipo `Group` ganha `cycle_snapshots_exists: boolean`; `Dialog` de confirmação passa a renderizar um `Alert` (`severity="warning"` sem competência fechada, `severity="info"` com) com texto condicional; `handleDeleteGroup` agora guarda a mensagem retornada pelo backend (`res.data.message`) em `removeSuccessMessage` e o `Snackbar` exibe essa mensagem em vez do texto fixo anterior. Testes atualizados/novos em `frontend/src/pages/Dashboard.test.tsx`: teste de exclusão passa a checar a mensagem dinâmica do backend; dois testes novos cobrindo o aviso de exclusão irreversível (`cycle_snapshots_exists=false`) e o aviso de preservação de histórico (`cycle_snapshots_exists=true`). `cd frontend && npx tsc --noEmit` — sem erros. `npx vitest run src/pages/Dashboard.test.tsx` — 14 passed. `npx vitest run` (suíte completa) — 117 passed (18 arquivos). Verificação em browser real feita depois, durante a TASK-186 (ver log da TASK-186) — confirmou o texto exato do caso "sem competência fechada" end-to-end. | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-185` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `fcc65ffe2`. Fecha o plan.md §2 por completo (backend + frontend). |
| TASK-186 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `frontend/src/App.tsx`: rota `/meus-grupos` registrada com `element={<Dashboard />}` (mesmo componente, sem renomear arquivo); rota `/dashboard` passa a `element={<Navigate to="/meus-grupos" replace />}`, dentro do mesmo `<Route element={<SimpleShellLayout />}>` protegido por `RequireAuth` de antes. `cd frontend && npx tsc --noEmit` — sem erros. `npx vitest run` (suíte completa) — 117 passed (18 arquivos, nenhum importa `App.tsx` diretamente, então nenhum teste dependia do path antigo). **Verificação end-to-end em browser real** (dev server já rodando em `localhost:3000`/`localhost:8000`): registrado usuário de teste via `POST /api/register` + `POST /api/login`, criado grupo via `POST /api/groups`, `accessToken` setado em `localStorage`. Navegar para `http://localhost:3000/dashboard` resultou em `window.location.href === 'http://localhost:3000/meus-grupos'` (redirect confirmado); a página renderizou a tabela "Meus Grupos" com o grupo criado; clicar em "Excluir grupo" abriu o modal com o texto exato de exclusão física (`Esta ação é irreversível: ...`, confirmando a TASK-185 ao vivo); confirmar disparou `DELETE http://localhost:8000/api/groups/6112 → 200 OK` com corpo `{"message":"Grupo excluído permanentemente."}`, e a linha sumiu da tabela. Único ponto não atualizado nesta task (esperado, é o escopo da TASK-187): o link "Meus Grupos" do menu lateral ainda aponta para `/dashboard` (funciona via redirect, mas ainda não foi trocado para o path novo). | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-186` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `39ab9d29a`. |
| TASK-187 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `navigate('/dashboard')`/`to: '/dashboard'` trocado por `/meus-grupos` em `frontend/src/layouts/accountSettingsNavItems.ts:8`, `frontend/src/pages/LoginPage.tsx:38`, `frontend/src/pages/GroupForm.tsx` (2 ocorrências, cancelar e salvar) e `frontend/src/pages/GroupMembersForm.tsx:160`. `cd frontend && npx tsc --noEmit` — sem erros. `npx vitest run` (suíte completa) — **2 arquivos falharam, 3 testes vermelhos** (`SimpleShellLayout.test.tsx`, `GroupShellLayout.test.tsx` — ambos afirmam `href="/dashboard"` para o link "Meus Grupos"/o heading da rota antiga); regressão **esperada e intencional**, é exatamente o escopo da TASK-188 (próxima), que atualiza esses testes para `/meus-grupos`. Verificação ao vivo no browser (mesmo dev server da TASK-186, usuário/token reaproveitados): navegar para `/meus-grupos` confirma via `read_page` que o link "Meus Grupos" do menu lateral aponta para `href="/meus-grupos"` (antes apontava para `/dashboard`). | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-187` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `8393c66f5`. Checklist de `04-implementation.md` para frontend exige só `tsc` limpo (não lista `vitest` como obrigatório, diferente do backend que exige `php artisan test` verde) — os 3 testes vermelhos ficam registrados aqui para não serem esquecidos e são corrigidos na TASK-188, já planejada para isso. |
| TASK-188 | Implementada na branch da feature | 20260822 | isacaguiar (IA) | `frontend/src/layouts/SimpleShellLayout.test.tsx`, `frontend/src/components/RequireAuth.test.tsx` e `frontend/src/layouts/GroupShellLayout.test.tsx`: todas as ocorrências de `/dashboard` (rotas registradas, `initialEntries` e a asserção de `href` do link "Meus Grupos") trocadas para `/meus-grupos`; `Sidebar.test.tsx` deixado intocado por ser fixture genérica sem relação com rotas reais, conforme decidido em `plan.md`. Novo `frontend/src/App.test.tsx` renderiza `<App />` real (não uma reimplementação) dentro de `MemoryRouter initialEntries={['/dashboard']}`, com `axios` mockado e `accessToken` em `localStorage`, e confirma que a página de grupos ("Buscar grupo") renderiza — prova que o redirect de `App.tsx` funciona de ponta a ponta, não só a existência da rota. `cd frontend && npx tsc --noEmit` — sem erros. `npx vitest run` (suíte completa) — **118 passed (19 arquivos)**, os 3 testes da TASK-187 voltam a ficar verdes. | Branch de task `backend/20260822-melhoria-grupo-conta-participantes-TASK-188` criada a partir da branch da feature, mergeada de volta com `--no-ff` (sem PR) e removida. Commit da task `9adc4f883`. **Todas as 8 tasks da feature (TASK-181 a TASK-188) estão integradas na branch `backend/20260822-melhoria-grupo-conta-participantes`.** Checklist de integração rodado mais uma vez na branch já integrada (item 5 de `04-implementation.md`): backend `./vendor/bin/pint --test` — mesmos 9 issues pré-existentes de sempre, nenhum novo; `php artisan test` — 176 passed (514 assertions); frontend `npx tsc --noEmit` — sem erros; `npx vitest run` — 118 passed (19 arquivos). Feature pronta para abrir o PR único contra `dev` (gate humano de merge pendente). |
