# Implementation — Notificações in-app (e foto de perfil)

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260903

---

## 1. Desvios do fluxo padrão (se houver)

Nenhum. Segue `docs/sdd/04-implementation.md` §1 sem exceção (branch da feature `feature/20260903-notificacoes-in-app` a partir de `dev`, 1ª task direto nela, demais em sub-branch com `git merge --no-ff`, um único PR contra `dev` no fim).

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-260 | Concluída | 2026-09-03 | IA | `php artisan migrate --force` → `2026_09_03_000000_create_ex_notifications_table` DONE (363ms). `./vendor/bin/pint --test` nos 3 arquivos novos → PASS. `php artisan test --filter=NotificationModelTest` → 4 passed (8 assertions). `php artisan test` (suíte completa) → 289 passed (856 assertions). | Feita direto na branch `feature/20260903-notificacoes-in-app` (1ª task, sem sub-branch). `./vendor/bin/pint --test` na base inteira acusa 8 issues **pré-existentes** em `dev` (`PixPayload.php`, `Expense.php`, `User.php`, migrations de 2025) — não tocados nesta task ("não corrigir de passagem", `06-context-backend.md`). |
| TASK-261 | Concluída | 2026-09-03 | IA | `NotificationController` (index/unreadCount/markRead) + 3 rotas em `jwt.auth` de `routes/api.php`. `php artisan route:list --path=notifications` → 3 rotas registradas. `./vendor/bin/pint --test` nos 3 arquivos → PASS. `php artisan test --filter=NotificationControllerTest` → 7 passed (23 assertions). `php artisan test` (suíte completa) → 296 passed (879 assertions). | Sub-branch `feature/20260903-notificacoes-in-app-TASK-261`, merge `--no-ff` na branch da feature. Escopo por `user_id` (sem `authorizeGroupMembership` — a notificação é do destinatário). |
| TASK-262 | Concluída | 2026-09-03 | IA | `App\Support\Notifier` (guard best-effort + `expensePaid`/`settlementConfirmed`). Gatilhos inline: `pay()` captura `$wasPaid` antes do `update` e notifica só se `!$wasPaid`; `confirmSettlement()` notifica só se `$confirmation->wasRecentlyCreated`. `./vendor/bin/pint --test` nos 3 arquivos → PASS. `php artisan test --filter=NotifierTriggersTest` → 5 passed (23 assertions). `php artisan test` (suíte completa) → 301 passed (902 assertions). | Sub-branch `...-TASK-262`, merge `--no-ff`. Teste de best-effort do fluxo completo removido (byte inválido em UTF-8 não passa no `description` sob MySQL strict) — o guard() é coberto direto por `test_notifier_guard_swallows_a_write_failure`. |
| TASK-263 | Concluída | 2026-09-03 | IA | `Notifier::cycleSettled` (todos os membros) e `cycleClosed` (membros menos o ator). `sealCycleIfSettled()` lê `$wasSealed` do snapshot antes do `updateOrCreate` e só notifica na transição não-selado→selado. `close()` lê `$wasManuallyClosed` (`whereNotNull closed_manually_at`) antes e só notifica se `!$sealed && !$wasManuallyClosed`. `./vendor/bin/pint --test` → PASS. `php artisan test --filter=NotifierTriggersTest` → 9 passed (42 assertions). `php artisan test` (suíte completa) → 305 passed (921 assertions). | Sub-branch `...-TASK-263`, merge `--no-ff`. `cycle_settled` pode ser gravada durante um GET `/summary` (selagem preguiçosa) — decidido no plan §3, é idempotente. |
| TASK-264 | Concluída | 2026-09-03 | IA | `Notifier::groupMemberAdded` + chamada em `GroupMemberController::store()` após `attach` (`use App\Support\Notifier`). Sem guarda extra — o passo 4 já devolve 409 para membro repetido. `./vendor/bin/pint --test` → PASS. `php artisan test --filter=NotifierTriggersTest` → 11 passed (50 assertions). `php artisan test` (suíte completa) → 307 passed (929 assertions). | Sub-branch `...-TASK-264`, merge `--no-ff`. |
| TASK-265 | Concluída | 2026-09-03 | IA | `Notifier::expenseCreated` + chamada em `ExpenseController::store()` logo após `DB::commit()`. Destinatários = `payers` menos `user_creator_id`. `./vendor/bin/pint --test` → PASS. `php artisan test --filter=NotifierTriggersTest` → 13 passed (59 assertions). `php artisan test` (suíte completa) → 309 passed (938 assertions). | Sub-branch `...-TASK-265`, merge `--no-ff`. Chamada dentro do `try` mas depois do `commit`: `guard()` nunca deixa propagar, e `rollBack()` após `commit()` seria no-op. Encerra o plan §3 (gatilhos completos). |
| TASK-266 | Concluída | 2026-09-03 | IA | `frontend/src/hooks/useUnreadNotificationsCount.ts` (1º `setInterval` do app, 60s, 401 silencioso). `GroupShellLayout` chama o hook e passa `unreadCount`; `GroupHeader` ganha prop `unreadCount?` (default 0) e `<Badge badgeContent color="error" max={99}>` no sino. `GroupShellLayout.test.tsx` ganha o mock de `/api/notifications/unread-count`. `npx tsc --noEmit` → sem erro. `npx vitest run` (suíte completa) → 34 files, **208 passed**. | Sub-branch `...-TASK-266`, merge `--no-ff`. Prop `unreadCount` ficou **opcional com default 0** (plan §4 dizia obrigatória) — evita alterar 5 testes de `GroupHeader` alheios; TS continua estrito. Verificação visual no browser fica para a verificação end-to-end do fim da feature (precisa de backend + sessão). |
| TASK-267 | Concluída | 2026-09-03 | IA | `frontend/src/components/notificationText.ts` (`notificationText` por `type` + `formatRelative` com `Intl.RelativeTimeFormat`). `frontend/src/components/NotificationsMenu.tsx` (`Menu` ancorado no sino, busca `GET /api/notifications?page=1` só ao abrir, "Marcar todas como lidas" → `POST .../read` sem body + `onRead` + reload, clique no item → `POST .../read {id}` + `navigate('/groups/{groupId}/summary')` + `onClose`, estados loading/erro/vazio, `Snackbar` de erro). `GroupHeader` abre o menu pelo `onClick` do sino (novo state `notifAnchor`) e ganha prop `onNotificationsRead?`; `GroupShellLayout` passa `refetchUnread`. `GroupHeader.test.tsx` reescrito para mockar `NotificationsMenu` (evita dep de `<Router>`/axios). `npx tsc --noEmit` → sem erro. `npx vitest run` (suíte completa) → 36 files, **224 passed**. | Sub-branch `...-TASK-267`, merge `--no-ff`. `onNotificationsRead` também opcional (noop default), mesma razão da TASK-266. Encerra o plan §4 (frontend do sino). |
| TASK-268 | Concluída | 2026-09-03 | IA | Migration `2026_09_03_000001_add_photo_path_to_ex_users_table` (aditiva, `php artisan migrate --force` → DONE). `App\Support\AvatarStorage` (disco `local`, `avatares/<userId>/<uuid>`). `UserController::uploadPhoto`/`deletePhoto` (`foto` `image\|max:5120`, apaga a anterior, responde `avatar_url` resolvido). `UserPhotoController::show` (rota assinada `user.photo`, fora de `jwt.auth`, ADR-005). `User::getAvatarUrlAttribute` — foto enviada > `avatar_url` do Google > null; `photo_path` em `$hidden`. `php artisan route:list` → 3 rotas. `./vendor/bin/pint --test` nos arquivos novos → PASS. `php artisan test --filter=UserPhotoTest` → 8 passed (25 assertions). `php artisan test` (suíte completa) → **317 passed** (963 assertions). | Sub-branch `...-TASK-268`, merge `--no-ff`. `app/Models/User.php` já estava pint-dirty em `dev` (`single_line_after_imports`) — confirmado por `git stash`; não corrigido de passagem. |
