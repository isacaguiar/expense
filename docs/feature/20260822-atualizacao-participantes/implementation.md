# Implementation — Convite de Participante por E-mail

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260822

---

## 1. Desvios do fluxo padrão (se houver)

Feature segue `04-implementation.md` sem exceção. Branch da feature: `backend/20260822-atualizacao-participantes` (nasceu de `dev` atualizada; TASK-189 implementada direto nela, por ser a primeira task).

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-189 | Concluída | 2026-08-22 | isacaguiar (IA) | `GroupMemberController::store` passa a gerar token via `Cache::put('invitation-token:'.$email, $token, now()->addDays(2))` (`bin2hex(random_bytes(32))`), substituindo `Password::getRepository()->create($user)`. Teste novo `tests/Feature/GroupMemberInvitationTokenTest.php` (viagem no tempo com `travelTo` confirma o token válido pouco antes de 2 dias e expirado pouco depois, e confirma `ex_password_reset_tokens` sem linha para o novo e-mail). `./vendor/bin/pint --test app/Http/Controllers/GroupMemberController.php tests/Feature/GroupMemberInvitationTokenTest.php` — PASS (2 files). `php artisan test --filter="GroupMemberInvitationTokenTest\|GroupMemberControllerTest"` — 4 passed (15 assertions). `php artisan test` (suíte completa) — 182 passed (566 assertions). | E-mail de convite (`UserInvitedMail`) continua com a view quebrada — corrigido só na TASK-190, propositalmente fora do escopo desta task; teste novo usa `Mail::fake()` para isolar. |
| TASK-190 | Concluída | 2026-08-22 | isacaguiar (IA) | `UserInvitedMail` passa a receber `$inviter` (4º parâmetro do construtor) e renderizar a view `email.invitation` (em vez de `emails.user_invited`, inexistente), com `inviterName`, `groupName`, `inviteMessage` (null) e `activationLink` apontando para `/aceitar-convite?email=...&token=...`. `GroupMemberController::store` passa `auth()->user()` como inviter. `email/invitation.blade.php` ajustado para citar o grupo quando `$groupName` está presente (`@isset`), mantendo compatível com o fluxo antigo `InvitationController::invite` (que não passa `groupName`) até a TASK-192 removê-lo. Testes novos `tests/Feature/GroupMemberInvitationMailTest.php` — um sem `Mail::fake()` (MAIL_MAILER=array renderiza a view de verdade, garante que não quebra) e um com `Mail::fake()` + `Mail::assertSent` conferindo `inviter`/`group` corretos. `./vendor/bin/pint app/Mail/UserInvitedMail.php app/Http/Controllers/GroupMemberController.php tests/Feature/GroupMemberInvitationMailTest.php` — FIXED (1 style issue, autofix do próprio Pint) e depois limpo. `php artisan test --filter="GroupMemberInvitationMailTest\|GroupMemberInvitationTokenTest\|GroupMemberControllerTest\|InvitationController"` — 16 passed (42 assertions). `php artisan test` (suíte completa) — 184 passed (568 assertions). | `InvitationController::verify` ainda valida o token antigo (`Hash::check` contra a senha temporária) — adaptar para o novo `invitation-token:{email}` é a TASK-191, não tocado aqui. |
| TASK-191 | Concluída | 2026-08-22 | isacaguiar (IA) | `InvitationController::verify` troca a checagem `Hash::check($request->token, $user->password)` (mecanismo antigo do `invite()`) por `Cache::get('invitation-token:'.$user->email)` comparado ao token recebido — mesmo padrão já usado para `password-reset-token`. Mantém intacto o ramo `isResetTokenValid` (`forgotPassword`, fora de escopo). Token de convite agora também é de uso único (`Cache::forget` após sucesso). Testes novos `tests/Feature/InvitationControllerInvitationTokenTest.php` — token válido define senha e confirma e-mail; token consumido não pode ser reusado (401 na segunda tentativa); token errado não altera a senha (401); token expirado (viagem no tempo além de 2 dias) retorna 401. `./vendor/bin/pint app/Http/Controllers/InvitationController.php tests/Feature/InvitationControllerInvitationTokenTest.php` — PASS (2 files). `php artisan test --filter="InvitationControllerInvitationTokenTest\|InvitationControllerForgotPasswordTest\|InvitationControllerMailViewsTest\|InvitationControllerValidationTest"` — 14 passed (34 assertions). `php artisan test` (suíte completa) — 188 passed (577 assertions). | `InvitationController::invite` (o gerador do token antigo baseado em senha hasheada) continua existindo até a TASK-192 removê-lo — por isso o ramo antigo não foi simplesmente apagado, só teve sua fonte de verificação trocada. |
| TASK-192 | Concluída | 2026-08-22 | isacaguiar (IA) | Removidos `InvitationController::invite` e a rota `POST /invitations` (`routes/api.php`). `email/invitation.blade.php` simplificado — `groupName` volta a ser obrigatório na view (o `@isset`/`@else` de compatibilidade da TASK-190 só existia para o `invite()` antigo, que não passava esse dado). Testes obsoletos removidos: `InvitationControllerMailViewsTest::test_invite_with_message_resolves_the_real_view` e `InvitationControllerValidationTest::test_invite_with_already_registered_email_returns_422_not_500` (cobriam só o endpoint removido). `./vendor/bin/pint app/Http/Controllers/InvitationController.php routes/api.php tests/Feature/InvitationControllerMailViewsTest.php tests/Feature/InvitationControllerValidationTest.php` — FIXED (1 style issue, import não usado removido pelo próprio Pint) e depois limpo. `php artisan route:list --path=invitations` — só lista `POST api/invitations/verify` (confirma que `POST /invitations` não existe mais). `php artisan test --filter="InvitationController"` — 12 passed (32 assertions). `php artisan test` (suíte completa) — 186 passed (575 assertions). | Nenhuma. Coluna `invited_by` em `ex_users` (só usada pelo `invite()` removido) não foi tocada — remover coluna seria migration destrutiva, fora do escopo/gate desta task. |
