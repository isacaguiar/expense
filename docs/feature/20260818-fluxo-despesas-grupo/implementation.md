# Implementation — Fluxo de Despesas do Grupo

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260818

---

## 1. Desvios do fluxo padrão (se houver)

TASK-033 a TASK-038 seguiram `04-implementation.md` na versão anterior (branch por task a partir de `dev`, PR direto contra `dev`) — última feature a seguir esse modelo por completo (`ADR-003`). A partir de TASK-042, a mesma pasta de feature passa a usar o fluxo novo do `ADR-003` (branch única da feature `backend/20260818-fluxo-despesas-grupo`, tasks mergeadas nela localmente sem PR, um único PR feature→`dev` no final) — não é retroativo às tasks já mergeadas, só vale para o trabalho novo (tipos de despesa). Único ponto de atenção adicional: as tasks de backend exigem o container `mysql-expense-db` rodando localmente para migration/teste.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-033 | Concluída (PR #12 mergeado em `dev`, promovido pra `main` no PR #13) | 2026-08-18 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual no browser: login com usuário de teste (`isac@email.com`, id 1, membro dos grupos 1 e 8), navegação para `/groups/1/expenses` — `read_network_requests` confirma `GET http://localhost:8000/api/groups/1/expenses?year=2026&month=8` disparando de verdade (404, esperado — endpoint só existe a partir da TASK-034), enquanto antes do fix a chamada nunca acontecia (`if (!groupId) return;` cortava antes). | Para o teste manual, foi necessário subir o container `mysql-expense-db` (estava parado) e definir uma senha conhecida (`senha123`) no usuário de teste local (id 1) via `tinker`, só no banco de dev local — não é dado de produção. |
| TASK-034 | Concluída (PR #14 mergeado em `dev`) | 2026-08-18 | Claude (IA) | `php artisan test tests/Feature/ExpenseControllerIndexByGroupTest.php` — 4/4 passou. `php artisan test` (suíte completa) — 18/18 passou. `curl` manual: membro do grupo 1 recebe `200` com as 4 despesas de jun/2025; não-membro do grupo 2 e grupo inexistente (999) recebem `404`; requisição sem `year`/`month` (com `Accept: application/json`, igual o `axios` envia) recebe `422`. `./vendor/bin/pint --test` nos arquivos tocados — só achados pré-existentes em `ExpenseController.php` (confirmado comparando com baseline antes da mudança), nada introduzido por esta task. | Revisão de segurança feita via agent `security-reviewer` antes do commit (rota nova expondo dado financeiro, regra do `CLAUDE.md`) — achados aplicados: (1) checagem de membership devolve `404` em vez de `403` (evita enumerar grupo existente pra quem não é membro, mesmo padrão de `GroupController::authorizeMembership`, extraído para `Controller::authorizeGroupMembership` pra reuso na TASK-036); (2) `auth()->id()` em vez de `auth('api')->id()` (alinha com o resto do projeto); (3) `year`/`month` validados via `$request->validate()`. Nenhum achado bloqueante de IDOR/SQL injection/vazamento de dado. |
| TASK-035 | Concluída (PR #15 mergeado em `dev`) | 2026-08-18 | Claude (IA) | **Sem mudança de código**: `ExpenseManager.tsx:82` já chamava `GET ${API_BASE_URL}/api/groups/${groupId}/expenses` desde antes desta feature — path idêntico ao registrado na TASK-034. Depois do fix de parâmetro (TASK-033) + endpoint existir (TASK-034), a integração já funciona. Validação manual: criada despesa de teste via `tinker` (`group_id=1`, `date_payment=2026-08-15`, pagador Isac) e depois soft-deletada (`deleted=true`) ao final; navegação para `/groups/1/expenses` no browser mostra a linha renderizada corretamente (`14/08/2026 · Despesa TASK-035 (teste manual) · 42.50 · Isac`), confirmando o critério de aceite. `read_network_requests` confirma `GET .../api/groups/1/expenses?year=2026&month=8 → 200`. | Task de validação, não de implementação — documentado aqui para manter o rastro exigido por `04-implementation.md`, mesmo sem diff de código de produção. |
| TASK-036 | Concluída (PR #17 mergeado em `dev`) | 2026-08-18 | Claude (IA) | `php artisan test tests/Feature/ExpenseControllerStoreTest.php` — 3/3 passou. `php artisan test` (suíte completa) — 21/21 passou. `curl` manual: membro do grupo 1 cria despesa (`201`) mesmo enviando `user_creator_id` de outro usuário no payload — registro criado confirma via `tinker` que `user_creator_id` ficou como o usuário autenticado (id 1), não o valor enviado (id 2); tentativa de criar despesa no grupo 2 (não-membro) retorna `404` e não cria registro. `./vendor/bin/pint --test` — só achados pré-existentes em `ExpenseController.php`, nada novo. | Revisão de segurança via agent `security-reviewer` antes do commit — nenhum achado bloqueante. Achado não-bloqueante confirmado (já previsto no plan.md §3): `user_payer_id`/`payers[]` continuam sem checagem de membership — formalizado como item 010 do backlog (`docs/backlog/expense-store-sem-checagem-membership-payer.md`), não bloqueia esta task. Achados informativos pré-existentes fora de escopo: `catch` de `store` vaza `$e->getMessage()` na resposta; `Group::findOrFail` não filtra `deleted=false` em nenhum método do controller (mesmo padrão em `indexByGroup`/`getMonthlyExpenses`) — não corrigidos aqui, fora do escopo desta task. |
| TASK-037 | Concluída (PR #19 mergeado em `dev`) | 2026-08-18 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual completa no browser: login, `/groups/1/expenses`, abrir modal "Nova Despesa" (seletor de pagador já vem populado com os membros reais do grupo — Isac, João, Maria — via `GET /api/groups/1/members`, e pré-selecionado com o usuário autenticado via `GET /api/me`), preencher descrição/valor/data, trocar o pagador para "João", salvar — `read_network_requests` confirma `POST http://localhost:8000/api/expenses → 201 Created`, modal fecha e a tabela atualiza sozinha mostrando a linha nova (`17/08/2026 · Despesa de teste TASK-037 · 88.90 · João`), sem reload manual. Registro confirmado via `tinker`: `user_creator_id=1` (usuário autenticado, não veio do payload), `user_payer_id=2` (João, selecionado no formulário) — depois soft-deletado (`deleted=true`). | Nenhuma mudança de backend — reaproveita `GET /api/groups/{id}/members` e `GET /api/me` já existentes. |
| TASK-042 a TASK-045 | Concluída (merge local na branch da feature, `464a3bb9`→`ffdfc471`, merge commit) | 2026-08-19 | Claude (IA) | `php artisan migrate --path=database/migrations/2026_08_19_010752_add_fixed_type_to_ex_expenses_table.php` — aplicado local com sucesso (399ms). `php artisan test` (suíte completa) — 39/39 verde. `./vendor/bin/pint --test` nos arquivos tocados — limpo, exceto achado pré-existente em `Expense.php` (confirmado via `git diff` que já existia antes desta mudança, não introduzido aqui — mesmo padrão da TASK-034/036). | Migration (`MODIFY COLUMN` no enum) é "alterar tipo de coluna" pela Constitution — destrutiva, gate humano explícito antes de rodar em ambiente compartilhado/produção; aplicação **local** (feita aqui) é autônoma. Decisão de projetar despesa Fixa via 2ª query + merge em PHP em vez de `UNION` (precisa de `with('payers')` em ambas). `getMonthlyExpenses` deliberadamente não tocado (specify §4, plan.md §9 — sem consumidor hoje). |
| TASK-046 | Concluída (mesmo merge acima) | 2026-08-19 | Claude (IA) | Testes novos: `ExpenseControllerStoreTest` (4 casos: `IN_INSTALLMENTS` multi-quota, `FIXED` simples, `FIXED` com `installments≠1` → 422, `FIXED` com >1 quota → 422), `ExpenseControllerStopRecurrenceTest` (4 casos: sucesso, não-membro → 404, tipo errado → 422, cutoff anterior à criação → 422), `ExpenseControllerIndexByGroupTest` (2 casos: despesa Fixa projetada em mês seguinte, some a partir do mês de corte inclusive). `php artisan test` — 19/19 nos arquivos novos/alterados, 39/39 na suíte completa. | Bug pego pelo próprio teste antes do commit: closure de `indexByGroup` não capturava `$mapRow` (`use ($monthStart)` faltava `$mapRow`) — `ErrorException: Undefined variable $mapRow`, corrigido antes de rodar de novo. |
| TASK-047 a TASK-052 | Concluída (merge local na branch da feature, `9586acdd`) | 2026-08-19 | Claude (IA) | `npx tsc --noEmit` — sem erros. `npx vitest run src/pages/ExpenseManager.test.tsx` — 7/7 verde; `npx vitest run` (suíte completa frontend) — 10/10 verde. Validação manual completa no browser (login `isac@email.com`, grupo 1, senha resetada localmente via `tinker` para o teste — mesmo padrão já usado nas TASK-033/037): (1) criada despesa parcelada "Sofá parcelado" 300,00 em 3 parcelas — `read_network_requests` confirma `POST /api/expenses → 201`; `tinker` confirma 3 quotas de R$100 cada, `paid:false`, `date_expected` incrementando 1 mês (19/08, 19/09, 19/10), `payers` com os 3 membros (default todos marcados, sem desmarcar nenhum neste teste); (2) criada despesa fixa "Aluguel" 1200,00 — `tinker` confirma `expense_type=FIXED`, `installments=1`, 1 quota `paid:false`, `fixed_recurrence_ends_at=null`; (3) navegando para setembro (mês seguinte), "Aluguel" aparece automaticamente na tabela com o mesmo dia do mês (18/09), confirmando a projeção de `indexByGroup`; (4) clicado "Remover despesa fixa" na linha de setembro, diálogo abriu com as 2 opções, clicado "A partir deste mês" — `POST /api/expenses/130/stop-recurrence → 200`, a linha some da tabela de setembro imediatamente (sem reload manual); (5) voltando para agosto (mês de criação), "Aluguel" continua aparecendo normalmente — confirma que o corte não afeta meses anteriores. Despesas de teste ("Sofá parcelado", "Aluguel") soft-deletadas via `tinker` ao final (`deleted=true`, não é dado de produção). | Seletor de participantes (multi-select de `payers[]`, achado do usuário durante o planejamento — specify §R7) não foi testado desmarcando um membro no browser (coberto pelos testes automatizados TASK-052, casos "uncheck a participant" e "blocks saving when no participant is selected"); validação manual focou nos 3 tipos de despesa e no ciclo completo de vida da despesa Fixa (criação → projeção → remoção → histórico preservado), que são os fluxos novos de maior risco. |
| TASK-038 | Concluída (PR #20 mergeado em `dev`) | 2026-08-18 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual completa no browser, três cenários: (1) usuário `isac@email.com` (id 1, membro dos grupos 1 e 8) navega para `/expenses` e vê a lista "Escolha um grupo" com os cards "Grupo Familiar" (`href=/groups/1/expenses`) e "Grupo 2" (`href=/groups/8/expenses`) — confirmado via `read_page`; (2) usuário de teste descartável (criado via `tinker`, id 95, sem nenhum grupo) navega para `/expenses` e vê a mensagem "Você ainda não participa de nenhum grupo." (`get_page_text` confirma, sem tela em branco); (3) mesmo usuário 95 após `attach` a exatamente 1 grupo (`tinker`) navega para `/expenses` e é redirecionado automaticamente — `window.location.pathname` confirma `/groups/1/expenses` sem interação do usuário. Usuário de teste 95 e seu vínculo de grupo removidos ao final (`tinker`, `detach()` + `delete()` — não é registro de negócio como grupo/despesa, criado só para este teste). | Nenhuma mudança de backend — reaproveita `GET /api/groups` já existente (mesma chamada que `GroupList.tsx` já fazia). Última task do `tasks.md` desta feature; promoção `dev`→`main` fica como passo separado, após validação em `dev` e aprovação humana explícita. |
| Checklist pré-PR (feature completa) | Verde | 2026-08-19 | Claude (IA), via agent `pr-readiness-checker` | `./vendor/bin/pint --test` — achados só em arquivos fora do diff desta feature (débito pré-existente confirmado contra `main`); `php artisan test` — 39/39 passou; `npx tsc --noEmit` — sem erros; `npx vitest run` — 10/10 passou; `git diff main...HEAD` revisado por padrão de segredo — nenhum encontrado; `php artisan migrate:status` confirma migration destrutiva (`2026_08_19_010752_add_fixed_type_to_ex_expenses_table`) aplicada só localmente (batch 5). | Corrigidas as etiquetas de status desatualizadas ("PR aberto") das linhas TASK-037/TASK-038 acima, apontadas pelo checklist. Se `dev` for ambiente compartilhado, a migration destrutiva ainda precisa do gate humano explícito antes de rodar lá — não decidido aqui. |
