# Implementation — Analytics no app e consentimento de cookies

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260920

---

## 1. Desvios do fluxo padrão (se houver)

Sem desvio de fluxo. Duas particularidades de verificação, por causa da natureza da feature:

- **O site não tem suíte automatizada.** `pint`/`phpunit` são do backend e `tsc`/`vitest` são do app; as tasks do site (309 a 312, 320) são verificadas por `php -l`, execução real no servidor local (`site-static` em `.claude/launch.json`, `localhost:4173`) e inspeção de rede/cookies no navegador.
- **"Nenhuma requisição ao Google" é critério de comportamento, não de código.** A prova registrada abaixo é a leitura de `document.scripts` (quantos scripts com `google` no `src` existem no DOM) e de `document.cookie` (se `_ga*` foi criado), não a leitura do fonte.
- **O painel de rede não serve como prova aqui.** Durante a TASK-311 ficou claro que `read_network_requests` não registra script de origem cruzada injetado dinamicamente: mesmo com o `gtag.js` comprovadamente carregado (cookie `_ga` criado), a listagem só mostrava requisições para `localhost`. Ausência no painel **não** é prova de ausência de requisição — por isso o critério migrou para DOM + cookie, que medem o efeito real.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-309 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l site/src/config.php` → "No syntax errors detected"; `php -r '$c = require "site/src/config.php"; echo $c["ga_measurement_id"];'` → `G-RNQM4DT19G`; `grep -n ga_measurement_id site/src/config.php` → linha 33 | Chave posicionada ao lado de `site_url`, com comentário cruzado apontando para `VITE_GA_MEASUREMENT_ID` no app (plan §0: o contrato é duplicado por não haver código compartilhado). Comportamento do site inalterado nesta task — o `header.php` ainda carrega o gtag como antes, é a TASK-310 que troca. |
| TASK-310 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l site/src/templates/header.php` → "No syntax errors detected"; `grep -c G-RNQM4DT19G site/src/templates/header.php` → `0`. No servidor local (`site-static`, `localhost:4173`), com os cookies do domínio zerados e recarregando: `document.cookie` → `(vazio)` (nenhum `_ga*` criado); `window.dataLayer` → uma única entrada, `consent default` com os quatro sinais `denied`. Repetido em `/precos.php`: mesmo estado, `typeof window.gtag` → `function`, console sem erros. | Dois achados durante a verificação: (1) havia cookies `_ga`/`_ga_RNQM4DT19G` **pré-existentes** em `localhost:4173`, resíduo de execução local anterior com o header antigo — foi preciso zerá-los para provar que nada novo é criado; (2) o bloco antigo ficava **antes** do `<meta charset>`, e a reescrita passou o charset para a primeira linha do `<head>`, onde ele deve estar. O ID vai para o JS por `json_encode` (contexto JavaScript), não por `e()` (contexto HTML). A leitura de `read_network_requests` feita nesta task foi descartada como prova na TASK-311 (ver §1): o cookie ausente é que sustenta a conclusão. |
| TASK-311 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l` limpo em `consent.php`, `footer.php` e `header.php`. Em `localhost:4173`, os cinco critérios: **(a)** sem cookie → `banner.hidden === false`, botões `["Recusar:denied","Aceitar:granted"]`; **(b)** clique em "Aceitar" → `document.cookie` = `scd_consent=granted` + `_ga=GA1.1...` + `_ga_RNQM4DT19G=...`, `document.scripts` passa a conter `https://www.googletagmanager.com/gtag/js?id=G-RNQM4DT19G`, `dataLayer` = `consent default` → `consent update granted` → `js` → `config`, tudo **sem recarregar**; **(c)** com cookie limpo, clique em "Recusar" → `scd_consent=denied`, `scripts` com `google` no `src` = `0`, nenhum `_ga`, `dataLayer` com `consent update denied`; **(d)** reload em `/recursos.php` com escolha gravada → banner escondido, `0` scripts do Google; **(e)** no HTML servido por `curl`: `<section class="consent-banner" ... hidden>`, `grep -c noscript` → `0`, `grep -c googletagmanager` → `0`. Console sem erros. | O banner sai do servidor com `hidden` e só o JS revela — sem script não há banner nem medição (falha fechada), ao contrário do `nav.js`. `Secure` no cookie é condicionado a `location.protocol === 'https:'`, senão a escolha não sobreviveria no servidor local em http. `window.scdConsent.open()` fica exposto para o botão do rodapé da TASK-312. Uma visita de teste foi registrada na propriedade real `G-RNQM4DT19G` ao validar o critério (b) — é inerente a provar que o aceite carrega o GA de verdade. |
| TASK-312 | Concluída | 2026-09-20 | Claude Opus 5 | `php -l site/src/templates/footer.php` → limpo. `curl` nas 8 páginas + `grep -c data-consent-open` → `1` em todas; `grep -c logo-novemax.png` → `1` em todas (o bloco do logo segue intacto). Em `/termos.php` com `scd_consent=granted`: botão visível com rótulo "Preferências de cookies", banner escondido, `1` script do Google carregado (a escolha persistida funciona). Clique no botão → banner `visível`. Clique em "Recusar" no banner reaberto → cookie vira `scd_consent=denied` e `dataLayer` recebe `consent update denied`. Recarregando: cookie `denied`, `document.scripts` com `google` no `src` = **0**, banner escondido (já há escolha), botão ainda visível. | O botão sai do servidor com `hidden` e é o `consent.js` que revela — sem JS ele não apareceria, em vez de aparecer morto. Botão morto no rodapé/tela já foi defeito neste produto (`docs/bugfix/20260830-login-remover-botao-microsoft.md`), e o comentário no `footer.php` registra o porquê. É `<button>` com aparência de link: a ação é reabrir o banner, não navegar, então não entra em `config.php['footer_nav']`, que renderiza `<a href>`. |
| TASK-313 | Concluída | 2026-09-20 | Claude Opus 5 | `cd frontend && npx tsc --noEmit` → sem erros. No dev server que já estava rodando em `localhost:3000` (do usuário, não subi outro), `await import('/app/src/config.ts')` → `GA_MEASUREMENT_ID` = `""`, `typeof` `string`, `API_BASE_URL` intacto em `http://localhost:8000`; `document.scripts` com `google` no `src` = `0`; console sem erros. | Default vazio: sem `VITE_GA_MEASUREMENT_ID` o analytics fica desligado e o tráfego de desenvolvimento não entra na propriedade de produção. O tipo foi anotado explicitamente (`: string`) porque `import.meta.env` só tem índice genérico via `vite/client` e o valor sairia como `any` — a convenção do projeto é não deixar `any` não justificado. Alternativa descartada por ser escopo maior: declarar `ImportMetaEnv` completo em `src/vite-env.d.ts`, o que também re-tiparia `VITE_API_BASE_URL`. |
