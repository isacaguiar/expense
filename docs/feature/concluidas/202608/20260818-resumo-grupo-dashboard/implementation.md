# Implementation — Resumo do Grupo (Dashboard)

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.2 · Criado em: 20260818

---

## 1. Desvios do fluxo padrão (se houver)

Branch da feature (`backend/20260818-resumo-grupo-dashboard`) criada a partir de `dev` local já com os 3 documentos (`specify.md`/`plan.md`/`tasks.md`) commitados diretamente em `dev` antes da branch existir — o usuário precisou commitar ali para trazer outras alterações remotas sem perder o trabalho em andamento (`dev` não estava, até esse ponto, publicado em `origin/dev`). A branch da feature nasceu desse ponto, então tecnicamente carrega esses 2 commits de documentação como base — sem efeito prático, já que fazem parte do histórico comum. TASK-056 renumerada de `TASK-053` para `TASK-056` antes de começar a implementação: a branch trazida pelo merge (`backend/20260819-fluxo-despesas-correcoes-review`, PR #26) já tinha ocupado `TASK-053`–`TASK-055` em `docs/feature/concluidas/202608/20260818-fluxo-despesas-grupo/tasks.md`.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-056 | Concluída (commit `182b78fa`, direto na branch da feature — primeira task, sem sub-branch, `ADR-003`) | 2026-08-19 | Claude (IA) | `php artisan migrate --path=database/migrations/2026_08_19_020000_add_closing_day_to_ex_groups_table.php` — aplicado local (422ms). `tinker`: `Group::create([...'closing_day'=>10])` persiste `closing_day=10`; grupo sem `closing_day` persiste `NULL` (regressão ok). | Nenhum `->delete()` físico usado nos testes (lição da própria sessão — ver observação da TASK-057). |
| TASK-057 | Concluída (merge `08597a3a`) | 2026-08-19 | Claude (IA) | `php artisan test --filter=GroupControllerTest` — 10/10 verde (4 casos novos de `closing_day`). `php artisan test` (suíte completa) — 60/60 verde. | Durante a validação manual da TASK-056, um `Group::delete()` em `tinker` apagou fisicamente 2 registros de teste (`Group` não usa `SoftDeletes` — exclusão do app é sempre via `update(['deleted'=>true])`, regra do `CLAUDE.md`). Eram só linhas dummy criadas na mesma sessão, sem dado de produção envolvido; passou a usar `update(['deleted'=>true])` para toda limpeza manual daqui em diante. |
| TASK-058 | Concluída (merge `cd737203`) | 2026-08-19 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual no browser: grupo de teste criado via `tinker` (`closing_day=10`) — campo carrega "10" ao editar; alterado para "15" e salvo — `tinker` confirma `closing_day=15` persistido via `PUT /api/groups/{id}`. | — |
| TASK-059, TASK-060 | Concluída (merge `6b860eb6`) | 2026-08-19 | Claude (IA) | `php artisan test --filter=BillingCycleTest` — 8/8 verde (calendário default, `closing_day` custom, não-fechado no próprio dia de corte, fechado no dia seguinte, clamp fevereiro comum e bissexto, `cyclesAgo`). | — |
| TASK-061, TASK-062 | Concluída (merge `18917591`) | 2026-08-19 | Claude (IA) | `./vendor/bin/pint --test` nos arquivos tocados — limpo. Validação manual via `curl` com dados reais (3 despesas: À Vista, Parcelada 3x, Fixa) num grupo com `closing_day=15`: ciclo calculado `16/jul–15/ago`; parcela mostrou valor da quota (R$300) em vez do total (R$900); despesa Fixa projetou corretamente em 05/ago; `totals` bateu (1100/300/800); `balances` somou zero (payer +550, participante -550); `cycles_ago=1` navegou para o ciclo anterior corretamente; usuário não-membro recebeu `404`. `php artisan test --filter=ExpenseControllerSummaryTest` — 6/6 verde (28 assertions). `php artisan test` (suíte completa) — 60/60 verde. | Dados de teste (grupos 366/417, despesas 189-191, usuários `qa.*@example.com`) soft-deletados (`update(['deleted'=>true])`) ao final de toda a implementação da feature (ver linha "Limpeza" abaixo). |
| TASK-063 | Concluída (merge `d86420b6`) | 2026-08-19 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual no browser: cabeçalho e cards batendo com a API; navegação "ciclo anterior"/"próximo ciclo" funcionando, com "próximo" desabilitado no ciclo mais recente. | **Bug encontrado e corrigido durante a validação**: `new Date('YYYY-MM-DD')` interpreta a string como UTC-meia-noite — em fuso `America/Sao_Paulo` (UTC-3) exibia a data com 1 dia a menos ("15 de jul" para um ciclo que começava em 16/jul). `formatDate` corrigida para construir a `Date` a partir dos componentes `year/month/day` locais. O mesmo padrão (`new Date(exp.date).toLocaleDateString(...)`) existe em `ExpenseManager.tsx` e não foi tocado (fora do escopo desta feature) — registrado como item 013 do backlog (`docs/backlog/expense-manager-data-exibida-com-um-dia-a-menos.md`). |
| TASK-064 | Concluída (merge `e7265967`) | 2026-08-19 | Claude (IA) | Validação manual no browser (`get_page_text`): lista mostrou "Geladeira/Pendente", "Mercado/Paga", "Aluguel/Pendente" — status batendo com `paid` da API. | — |
| TASK-065 | Concluída (merge `6c645fa4`) | 2026-08-19 | Claude (IA) | Validação manual no browser: "QA Resumo R$550,00 a receber" / "QA Membro 2 R$550,00 a pagar" — batendo com `balances[]`. | — |
| TASK-066 | Concluída (merge `def8005a`) | 2026-08-19 | Claude (IA) | Validação manual no browser: segundo grupo de teste criado via `tinker`; selecionar no dropdown navegou para `/groups/417/summary` e recarregou cabeçalho/cards/lista vazia/saldo zero corretamente. | — |
| TASK-067 | Concluída (merge `6ae770a9`) | 2026-08-19 | Claude (IA) | `npx tsc --noEmit` — sem erros. Validação manual nos 3 cenários: usuário com 2 grupos → tela de escolha com links corretos; usuário com exatamente 1 grupo (criado via `tinker`) → redirect automático confirmado via `window.location.pathname`; usuário com 0 grupos (criado via `tinker`) → mensagem "Você ainda não participa de nenhum grupo.". | Login via UI apresentou instabilidade pontual no clique do botão "Entrar" nesta sessão (retry necessário até o `POST /api/login` disparar) — resolvido obtendo o token via `curl` direto e injetando em `localStorage` para os cenários seguintes, mais confiável que repetir cliques na UI. |
| TASK-068 | Concluída (merge `5af19d55`) | 2026-08-19 | Claude (IA) | Validação: clique no ícone via DOM real (`button.click()` no elemento `SummarizeIcon`) navegou para `/groups/366/summary`, confirmado por `window.location.pathname`. | O clique via coordenada da ferramenta de browser (`computer` click por `ref`) não disparou a navegação nesta sessão (mesma instabilidade da TASK-067); o clique real no elemento DOM confirma que o `onClick` do componente está correto — não é um bug do código. |
| TASK-069 | Concluída (merge `4753c810`) | 2026-08-19 | Claude (IA) | `npx vitest run` (suíte completa) — 21/21 verde (`GroupSummary.test.tsx`: 5 casos; `SummaryEntry.test.tsx`: 3 casos; `GroupForm.test.tsx`: 2 casos). `npx tsc --noEmit` — sem erros. | Ajuste necessário: `getByLabelText('Nome')`/`('Descrição')` falhava porque o MUI anexa um `*` ao texto do label em campos `required`, mudando o nome acessível para "Nome *" — trocado para `getByLabelText(/^Nome/)` (mesma causa-raiz não afeta `ExpenseManager.test.tsx` porque os campos lá não são `required`). |
| Checklist de integração (feature completa) | Verde | 2026-08-19 | Claude (IA) | `./vendor/bin/pint --test` — 13 achados, todos em arquivos fora do diff desta feature (débito pré-existente, confirmado comparando com o commit anterior à TASK-056). `php artisan test` — 60/60 verde. `npx tsc --noEmit` — sem erros. `npx vitest run` — 21/21 verde. | Dados de QA (grupos 366/417, despesas 189-191) soft-deletados via `tinker` (`update(['deleted'=>true])`) ao final — não são dado de produção, criados só para validação manual desta feature. Usuários de teste (`qa.*@example.com`) deixados no banco local (sem dado sensível/de negócio associado, sem custo de manter). Ainda não há PR aberto — branch local, aguardando decisão do usuário sobre publicar/abrir PR contra `dev`. |
