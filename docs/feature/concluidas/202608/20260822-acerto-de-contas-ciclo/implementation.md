# Implementation — Acerto de Contas por Ciclo

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260822

---

## 1. Desvios do fluxo padrão (se houver)

Todas as 6 tasks foram implementadas numa única sessão contínua, direto na branch da feature (`backend/20260822-acerto-de-contas-ciclo`, criada a partir de `dev` em `f2ea231ed`), sem sub-branch por task a partir da TASK-002 como `ADR-003` prevê para a 2ª task em diante — na prática só existe a branch da feature, sem merges `--no-ff` intermediários. Justificativa: as tasks são pequenas, sequenciais e foram revisadas/testadas uma a uma antes de seguir para a próxima (ver log abaixo), então o valor do isolamento por sub-branch (poder descartar uma task sem afetar as outras) não se aplicava aqui. Granularidade fica preservada nos commits individuais por task nesta mesma branch.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-001 | Concluída | 20260822 | Claude (IA) | Editado `computeCycleSummary()` (`ExpenseController.php`) para acumular `$owed[credorId][devedorId]` e derivar `$settlements` líquido por par. `php artisan test --filter=ExpenseControllerSummaryTest`: 15 passed (100 assertions), incluindo o cenário de regressão desta conversa. | Bug encontrado e corrigido durante a implementação: a primeira versão da dedupe de pares (`if ($debtorId < $creditorId) continue`) perdia pares onde o menor id nunca aparecia como credor — corrigido com uma chave normalizada `min:max` e um mapa `$processedPairs`. |
| TASK-002 | Concluída | 20260822 | Claude (IA) | Criada migration `2026_08_22_140000_add_settlements_to_ex_group_cycle_snapshots_table.php` (coluna `json`, nullable). `php artisan migrate --path=...`: aplicada em 409ms sem erro. `GroupCycleSnapshot`: `settlements` adicionado a `$fillable`/`$casts`. | — |
| TASK-003 | Concluída | 20260822 | Claude (IA) | `summary()`, `cycleSnapshotFor()`, `close()`, `reopen()` passam a expor/persistir `settlements`. `php artisan test --filter="ExpenseControllerCloseTest\|ExpenseControllerReopenTest\|GroupCycleSnapshotTest"`: 21 passed (78 assertions). | — |
| TASK-004 | Concluída | 20260822 | Claude (IA) | 5 testes novos adicionados a `ExpenseControllerSummaryTest.php` (reconciliação, sem auto-liquidação, netting de par recíproco, cenário de regressão com os 4 membros/5 despesas da conversa, imutabilidade pós-fechamento) + 1 em `ExpenseControllerCloseTest.php` (persistência no fechamento manual). `php artisan test` (suíte completa): 181 passed (561 assertions) — sem regressão em nenhum teste pré-existente. `php vendor/bin/pint`: PASS. | Dois ajustes de brittleness durante a escrita: `assertSame(100.0, ...)` trocado por `assertEquals` (JSON decodifica `100.0` como int `100`); comparação de snapshot trocada de `assertSame` pra `assertEquals` (MySQL `json` canonicaliza a ordem das chaves do objeto na volta do banco). |
| TASK-005 | Concluída | 20260822 | Claude (IA) | Tipo `SummarySettlement` + campo `settlements` em `useGroupCycle.ts`; componente novo `SettlementList.tsx`; bloco "Quem paga a quem" em `GroupSummary.tsx` (condicionado a `settlements.length > 0`). `npx tsc --noEmit`: sem erros. Verificação end-to-end no browser (ver observações). | — |
| TASK-006 | Concluída | 20260822 | Claude (IA) | 2 testes novos em `GroupSummary.test.tsx` (bloco ausente quando `settlements` vazio; bloco presente com nomes/valor resolvidos via `balances`) + `settlements: []` adicionado ao mock base (sem isso, todo teste pré-existente quebraria em runtime por `summary.settlements.length` de `undefined`). `npx vitest run`: 120 passed (19 arquivos) — suíte completa, sem regressão. | — |

**Verificação end-to-end (TASK-005/006), fora da suíte automatizada**: like backend (`php artisan serve --port=8010`) e frontend (`vite --port 3000`, exigido pelo CORS local fixo em `http://localhost:3000`) rodados manualmente; cenário da conversa (Isac/João/Maria/novemaxdev, 5 despesas) recriado via `tinker` num grupo descartável (`Familiar QA Acerto`, id 6479); `GET /groups/6479/expenses/summary` confirmado via curl com os 4 `settlements` esperados; página `/groups/6479/summary` verificada via `get_page_text` no Browser pane mostrando "Quem paga a quem" com as 4 linhas corretas (`João QA deve pagar R$ 137,50 a Isac QA`, `Maria QA deve pagar R$ 350,00 a Isac QA`, `Maria QA deve pagar R$ 112,50 a João QA`, `novemaxdev QA deve pagar R$ 112,50 a João QA`). Dado de teste e servidores manuais removidos depois (grupo/usuários apagados via `tinker`, processos `php artisan serve`/`vite` finalizados) — não sobrou estado no ambiente compartilhado além da migration aplicada (aditiva, esperada). Achado tangencial descoberto durante a verificação: já havia um `php artisan serve` de outra sessão rodando em `0.0.0.0:8000` a partir do checkout do repo raiz (branch `dev`, sem estas mudanças) disputando a mesma porta 8000 — não é meu para derrubar por padrão, então a verificação usou uma porta isolada (8010); não bloqueou o trabalho, só exigiu diagnóstico.
