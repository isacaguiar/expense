# Implementation — Pagamento em ciclo fechado

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260902

---

## 1. Desvios do fluxo padrão

- **PR de desenho antecipado.** A pedido do usuário, o PR #129 (`backend/20260902-pagamento-ciclo-fechado`
  → `dev`) foi remanejado para **rascunho**, base `dev`, contendo só
  `docs/feature/concluidas/202609/20260902-pagamento-ciclo-fechado/` (specify + plan + tasks), antes de
  codar, para revisão do desenho. A implementação das tasks continua na mesma branch e o
  mesmo PR #129 vira o PR da feature (sai de rascunho quando o código estiver pronto).
- **Manuais fora desta feature.** `README.md` (reescrito), `MANUAL.md`, `MANUAL.pdf` e
  `manual-assets/` **não** entram nesta feature nem em nenhum commit desta branch. O
  commit `c4e9dbab4` (README + manual) foi movido para a branch dedicada
  `docs/20260902-readme-manual` (criada nesta sessão a partir de `dev`); o PR fica para
  quando o usuário decidir. Esses arquivos precisam ficar de fora do que os workflows de
  deploy (FTP/SSH) publicam.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-244 | Concluída | 2026-09-02 | IA (sessão) | `php artisan migrate` → `2026_09_02_000000_add_settled_at_...` DONE (422ms). Backfill conferido via tinker: `total=4 null_settled_at=0 stamped=4`. `php artisan test tests/Feature/ExpenseControllerCycleIsFullySettledTest.php tests/Feature/GroupCycleSnapshotTest.php` → 12 passed. `./vendor/bin/pint --dirty` → PASS (5 files). `php artisan test` (suíte cheia) → 251 passed / 15 failed — as 15 são pré-existentes (drift de data em `ExpenseControllerStoreTest`/`ShowUpdateDestroyTest`, sem `Carbon::setTestNow()`, batem no guard de `TASK-159`); baseline sem esta task = mesmas 15. | Migration aditiva `ex_group_cycle_snapshots.settled_at` (nullable) + backfill `= updated_at`. `GroupCycleSnapshot::isSealed()` + fillable/casts. Helpers privados `cycleIsFullySettled` / `sealCycleIfSettled` / `unsealIfBroken` em `ExpenseController` — ainda **não referenciados** (TASK-245+ fazem a fiação). Gate humano da migration em produção: pendente (só rodou em local). Débito tangencial: os 15 testes com data fixa são time-bombs — candidatos a `docs/backlog/`. |
| — (drift) | Concluída | 2026-09-02 | IA (sessão) | `setUp()` com `Carbon::setTestNow('2026-08-19')` em `ExpenseControllerStoreTest` e `ExpenseControllerShowUpdateDestroyTest`. `php artisan test` → 266 passed / 0 failed. | Não é TASK-244..255 — correção do débito dos 15 testes com data fixa, bundlada nesta branch a pedido do usuário. Commit `test(backend): congelar relogio ...`. |
| TASK-245 | Concluída | 2026-09-02 | IA (sessão) | Editado `pay()`/`unpay()` em `ExpenseController.php`: `+cycles_ago` (`nullable\|integer\|min:0`), removido `rejectIfCompetenceClosed`, `resolveQuotaForCurrentCompetence`→`resolveQuotaForCycle`, `sealCycleIfSettled` após `pay`, `unsealIfBroken` após `unpay`. Flip de `test_cannot_pay/unpay_in_a_manually_closed_cycle` → `test_can_...` (agora 200). +4 testes novos (pagar ciclo `closed` via `cycles_ago`, alvo por `cycles_ago`, selagem pós-pagamento, dessela pós-`unpay`). `php artisan test tests/Feature/ExpenseControllerPayTest.php` → 26 passed. `./vendor/bin/pint --dirty` → PASS. `php artisan test` → 270 passed / 0 failed. | Guard de `future` **omitido** (desvia do plan §1): com `cycles_ago >= 0`, `BillingCycle::cycleFor` nunca resolve para `future` — guard seria código morto. `route` de `unpay` inalterada (Laravel injeta o `Request`). |
| TASK-246 + TASK-247 | Concluída | 2026-09-02 | IA (sessão) | **Feitas juntas** (mesmo arquivo, acopladas: os testes de `confirmSettlement` dependem do `summary()` ao vivo). `confirmSettlement()`: `+cycles_ago`, guarda invertida (`$cycleIsClosed` = `closed` OU `closed_manually` ativo; senão 422 "só depois do fechamento"), 422 se selada, `sealCycleIfSettled` no fim. `summary()`: nova precedência selado→congelado / `closed`+`closed_manually` não-selado→**ao vivo** com selagem preguiçosa / resto igual; `+cycle.settled`. `sealCycleIfSettled` → `: bool` + guarda de ciclo vazio (não sela ciclo sem despesa/acerto — travaria `reopen()`). `cycleHistory()` `+whereNotNull('settled_at')`. `reopen()` recusa 422 se selado. `close()` sela se já quitado (devolve `closed`+`settled:true`), senão `closed_manually`+`settled:false`. `grossDebts()` `+cycle.settled`. Removido `cycleSnapshotFor()`. Testes: flip `test_cannot_confirm_in_a_manually_closed_cycle`→`test_can_...`; `createSettlementBetween` passa a fechar o ciclo (param `closeCycle`); +4 novos (recusa com ciclo aberto, `cycles_ago` p/ ciclo `closed` por data, selagem pós-confirmação, imutabilidade de ciclo selado); reescritos `ExpenseControllerCloseTest::test_summary_..._reflects_live_state`, `ExpenseControllerSummaryTest::test_sealed_cycle_settlements_are_immutable_...`; `ExpenseControllerCycleHistoryTest` fixtures ganham `settled_at`. `php artisan test` → 273 passed / 0 failed. `pint --dirty` → PASS. | Commit único cita as duas tasks (rastreabilidade preservada). `close()` selar ciclo vazio quebrava `reopen` → guarda de ciclo vazio em `sealCycleIfSettled`. |
| TASK-248 | Concluída | 2026-09-02 | IA (sessão) | Rota `GET /groups/{groupId}/expenses/focus-cycle` em `routes/api.php` (grupo `jwt.auth`). Método `focusCycle()` + const `FOCUS_LOOKBACK = 12`: varre `cycles_ago` 0→12, pula selado, exige ciclo fechado (`closed` OU `closed_manually` ativo), devolve o 1º `!cycleIsFullySettled` (mais recente), senão `{cycles_ago:0}`. `FocusCycleTest` (5 casos: mais recente pendente, zero sem pendência, selado é pulado, `closed_manually` vigente pendente → 0, não-membro 404). `php artisan test` → 278 passed / 0 failed. `pint --dirty` → PASS. | — |
| TASK-249 | Concluída | 2026-09-02 | IA (sessão) | `computeCycleSummary()`: `->sortBy('date')` → `->sortBy([['paid','asc'],['date','asc']])` (não pago primeiro, cronológico dentro do bloco). `attachSettlementConfirmations()`: `->sortBy(fn ($s) => [$s['confirmedAt'] === null ? 0 : 1, -$s['amount']])` (não confirmado primeiro, depois maior valor). +2 testes em `ExpenseControllerSummaryTest` (ordem de despesas e de acertos). Frontend conferido: `ExpenseManager.tsx` (só `.filter` por tipo), `Payments.tsx` (`.map` direto), `PayableSettlementList.tsx` (`.map` direto) — nenhum reordena por cima. `php artisan test` → 280 passed / 0 failed. `pint --dirty` → PASS. | — |
| TASK-256 | Concluída | 2026-09-02 | IA (sessão) | `BillingCycle`: `const GRACE_DAYS = 5`; novo `public static closesAt(Carbon $boundary): Carbon` (= `$boundary + GRACE_DAYS`); `statusOf()` fecha em `$referenceStart->gte(self::closesAt($end))` (era `$end->lt($referenceStart)`). `boundariesFor()` **intacto** (rollover segue no dia seguinte à fronteira). `cycle.closes_at` (`Y-m-d`) aditivo em `summary()`/`close()`/`reopen()`/`grossDebts()` de `ExpenseController`. `BillingCycleTest`: reescrito `test_cycle_closed_the_day_after_closing_day` → `test_previous_cycle_stays_open_through_grace_window_then_closes`; +3 casos (`null` fecha dia 5 do mês seguinte; custom `closing_day` fecha em D+5; `closesAt`/`GRACE_DAYS`). `php artisan test --filter=BillingCycleTest` → 13 passed (44 assertions). `./vendor/bin/pint app/Support/BillingCycle.php app/Http/Controllers/ExpenseController.php tests/Unit/BillingCycleTest.php` → PASS (3 files). `php artisan test` (full) → 283 passed / 0 failed. | Gate humano: muda em produção a data em que o ciclo vigente trava (dia 1 → dia 5 do mês seguinte, no caso padrão) — só rodou em local. |
| TASK-258 | Concluída (no-op) | 2026-09-02 | IA (sessão) | Auditados `ExpenseControllerCloseTest`, `ReopenTest`, `CycleFreezeTest`, `CycleHistoryTest`, `GroupCycleSnapshotTest`, `PayTest`, `FocusCycleTest`: todos congelam `now` no dia 15/19 (ou `2026-09-05` / `2026-09-15`) com despesas do mês anterior para o caso "ciclo fechado" — ≥5 dias além da fronteira, fora da janela de carência. `php artisan test` (full, com TASK-256 aplicada) → 283 passed / 0 failed. | Nenhum ajuste de código. A premissa do plan §9 ("a suíte fica vermelha") não se confirmou — a suíte já isola os casos "fechado" fora da carência. |
| TASK-257 | Concluída | 2026-09-02 | IA (sessão) | `focusCycle()`: `$inGrace = $cycle['status'] === 'open' && Carbon::now()->startOfDay()->gt($cycle['end'])`; guarda passa a `if (! $cycleIsClosed && ! $inGrace) continue;`. Docblock atualizado. `FocusCycleTest` +2 casos: ciclo de janeiro na carência (`now = 2026-02-02`, `closing_day` null) com quota pendente → `cycles_ago: 1`; mesmo cenário com a quota paga → `cycles_ago: 0`. `php artisan test --filter=FocusCycleTest` → 7 passed. `./vendor/bin/pint app/Http/Controllers/ExpenseController.php tests/Feature/FocusCycleTest.php` → PASS (2 files). `php artisan test` (full) → 285 passed / 0 failed. | `$inGrace` nunca é `true` para `$ago = 0` (ciclo vigente nunca tem `end` no passado). |
| TASK-250 | Concluída | 2026-09-02 | IA (sessão) | `useGroupCycle.ts`: novo effect em `[groupId]` faz `GET .../expenses/focus-cycle`, seta `cyclesAgo` do retorno (`catch → 0`), `finally → setFocusResolved(true)`; effect do summary ganha guarda `if (!groupId || !focusResolved) return` + `focusResolved` nas deps. `SummaryCycle` ganha `closes_at: string` e `settled: boolean` (backend já envia sempre). Novo `src/hooks/useGroupCycle.test.tsx` (2 casos: focus-cycle antes do summary + semeia `cycles_ago`; `catch` → 0). `npx vitest run` (suíte inteira) → 27 files / 183 passed; `useGroupCycle.test.tsx` → 2 passed. `npx tsc --noEmit` → limpo. | Testes existentes de `GroupSummary`/`ExpenseManager`/`Payments` seguem verdes sem tocar nos mocks — o `mockImplementation` deles rejeita `/focus-cycle` (URL desconhecida) e o hook cai no fallback `cyclesAgo=0`. `reload()` mantém o `cyclesAgo` atual (não re-resolve o foco). |
| TASK-252 | Concluída | 2026-09-02 | IA (sessão) | `usePaymentActions`: nova assinatura `(currentUserId, summary, cyclesAgo, reload)`; `cycleIsOpen` (não consumido em lugar nenhum) removido do retorno; `canPay`/`canUnpay` passam a gatear só por `!cycleIsFuture` (`status === 'future'`) — credor paga/despaga em `closed`/`closed_manually`/selado; `handlePay`/`handleUnpay` mandam `cycles_ago` no corpo (`{ cycles_ago }` JSON, ou campo no `FormData` quando há comprovante). `ExpenseManager.tsx`: `cycleIsOpen` → `cycleIsEditable` (mesma regra `status === 'open'`, agora nomeia só edição/exclusão) nos 3 usos; passa `cyclesAgo` ao hook. `Payments.tsx`: destrutura `cyclesAgo` do `useGroupCycle` e passa ao hook. Novo `usePaymentActions.test.tsx` (5 casos). Atualizados 2 testes existentes que afirmavam o contrato antigo: `Payments.test.tsx` (`body` do unpay agora `{ cycles_ago: 0 }`) e `ExpenseManager.test.tsx` ("marcar como paga" agora VISÍVEL em ciclo `closed` para o credor — renomeado). `npx tsc --noEmit` limpo. `npx vitest run` → 29 files / 190 passed. | — |
| TASK-259 | Concluída | 2026-09-02 | IA (sessão) | Novo `src/components/CycleClosingAlert.tsx`: props `{ summary }`. PRÉ (`<Alert severity="info">`) quando `status === 'open'` e hoje ∈ [`end`, `closes_at`) → "Este ciclo fecha em DD/MM…"; PÓS (`<Alert severity="warning">`) quando `!settled` e `status ∈ {closed, closed_manually}` e há `balance < 0` → "O ciclo fechou e ainda falta acertar: NOMES." Fora disso `null`; guardas contra `end`/`closes_at` ausentes (`NaN`). Datas via parse local (mesmo cuidado de fuso de `GroupSummary`). Render em `GroupSummary.tsx` (abaixo do seletor de ciclo, acima do `CycleDetailPanel`) e `ExpenseManager.tsx` (abaixo do seletor de competência). Novo `CycleClosingAlert.test.tsx` (5 casos, datas relativas a hoje). Fixtures de `GroupSummary.test.tsx` e `ExpenseManager.test.tsx` ganharam `closes_at`/`settled` no `cycle`. `npx tsc --noEmit` limpo. `npx vitest run` → 31 files / 199 passed. | O sino/badge de notificação continua fora (backlog ID 020). |
| TASK-251 | Concluída | 2026-09-02 | IA (sessão) | `PayableSettlementList`: props novas `canConfirm: boolean` + `cycleSettled: boolean`. Botões do devedor passam a `{isDebtor && canConfirm && …}`; quando `isDebtor && !canConfirm && !confirmed`, um `<Typography variant="caption">` no lugar ("Ciclo encerrado." se `cycleSettled`, senão "Disponível após o fechamento do ciclo."). `Payments.tsx` calcula `canConfirm = (status === 'closed' \|\| status === 'closed_manually') && !settled` e passa `cycleSettled={summary.cycle.settled}`. Novo `PayableSettlementList.test.tsx` (4 casos). 3 testes de `Payments.test.tsx` que exercitavam a ação do devedor com ciclo `open` ganharam `cycle: { status: 'closed' }` no fixture; fixture default de `cycle` ganhou `closes_at`/`settled`. `npx tsc --noEmit` limpo. `npx vitest run` → 30 files / 194 passed. | — |
| TASK-253 | Concluída | 2026-09-02 | IA (sessão) | Ordem trocada em `groupNavItems` (`GroupSidebar.tsx`) e `simpleNavItems.ts`: Home → Despesas → **Pagamentos → Participantes** → Relatórios → Configurações → Sair. Nome dos testes `GroupShellLayout.test.tsx` / `SimpleShellLayout.test.tsx` ajustado (só descrição — não havia assertiva de ordem posicional, só de `href`). `npx vitest run` → 203 passed. | — |
| TASK-254 | Concluída | 2026-09-02 | IA (sessão) | Novos `src/pages/MembersEntry.tsx` e `ReportsEntry.tsx` (cópias de `PaymentsEntry`, só muda o alvo do `navigate` para `/groups/:id/members` e `/groups/:id/reports`). `App.tsx`: rotas `/members` e `/reports` no bloco `SimpleShellLayout` + imports. `simpleNavItems.ts`: `Participantes` ganha `to: '/members'`, `Relatórios` ganha `to: '/reports'`. `SimpleShellLayout.test.tsx`: `href` de Participantes/Relatórios passa de `'#'` para as rotas reais; nome do teste de Relatórios ajustado. Novos `MembersEntry.test.tsx` / `ReportsEntry.test.tsx` (2 casos cada — redireciona pro grupo mais ativo; mensagem com 0 grupos). `npx tsc --noEmit` limpo. `npx vitest run` → 33 files / 203 passed. | — |
| TASK-255 | Concluída | 2026-09-02 | IA (sessão) | Bloco `> **Supersessão parcial (2026-09-02):**` adicionado no topo de `docs/feature/concluidas/202608/20260821-home-ciclo-corrente-navegacao-futura/specify.md` — a regra "Home abre no ciclo que contém hoje" vale só sem ciclo fechado pendente; `focus-cycle` (feature 20260902) passou a abrir no ciclo fechado/em carência não quitado mais recente. | — |
