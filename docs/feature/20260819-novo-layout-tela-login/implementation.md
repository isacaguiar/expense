# Implementation — Novo Layout da Tela de Login

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260819

---

## 1. Desvios do fluxo padrão (se houver)

A partir da `TASK-071`, as tasks são commitadas diretamente na branch da feature (`frontend/20260819-novo-layout-tela-login`), um commit por task, sem sub-branch `-TASK-0xx` própria — diferente do passo 1 de `04-implementation.md` (que pede sub-branch a partir da 2ª task em diante). Motivo: a `TASK-070` já nasceu direto na branch da feature (primeira task, conforme o fluxo padrão); criar sub-branches retroativamente para as tasks seguintes só pra depois fazer merge `--no-ff` de volta na mesma branch não muda o resultado final (mesma sequência linear de commits) e adiciona overhead sem ganho de rastreabilidade nesta feature pequena. Cada task continua sendo um commit individual, revertível e citado no log abaixo — a rastreabilidade por task é mantida, só o mecanismo de branch/merge é simplificado.

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| Ajuste 5 (paleta: `#17A37F`) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `grep -rn "1FA64A|168A3D" src/` — sem resultado (nenhuma referência hardcoded restante). `javascript_tool`: `getComputedStyle` do botão "Entrar" retorna `rgb(23, 163, 127)` (= `#17A37F`). Console limpo. | Troca de cor centralizada em `frontend/src/pages/login/colors.ts` (`primary`: `#1FA64A` → `#17A37F`; `primaryDark`: `#168A3D` → `#128468`), propaga para todos os componentes de login automaticamente (nenhum outro arquivo tinha a cor hardcoded). |
| Ajuste 4 (topo, logo maior, fonte/padding do form) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: ícone da logo `64px` (era `48px`), texto da logo `30px` (era `24px`); conteúdo do painel com `justifyContent: flex-start` (era `center`) e `top: 0` dentro do painel; `input#email` com `fontSize: 14px` (era `16px`, redução de ~2px) e `padding: 10px 12px` (era `16.5px 14px`). Console limpo. | Conteúdo do painel de branding volta a nascer do topo (como no mockup original) em vez de centralizado verticalmente; fonte e padding dos campos de e-mail/senha reduzidos junto com os demais textos do formulário (checkbox, links, botão Entrar, Google/Microsoft, "Cadastre-se") em ~2px cada. |
| Ajuste 3 (background full-width, logo/texto maiores) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: 1 elemento com `background-image` de `login-hero.png`, `position: absolute`, `background-size: 100%`, largura igual à do painel (`537.6px` = 100% do painel de `42%`). Console limpo. | Ilustração deixou de ser `<img>` no fluxo do flexbox e virou camada de background (`position:absolute`, ancorada embaixo, `backgroundSize:'100% auto'`) atrás de um wrapper de conteúdo com `zIndex:1` — não empurra mais os outros elementos. Logo (ícone `40→48px`, texto `1.25rem→1.5rem`) e headline (`1.125rem→1.375rem`, ainda menor que a logo) aumentados a pedido do usuário. |
| Ajuste 3 (imagem sai do background, troca de asset, base no rodapé) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: `heroImg.parentElement` mede a mesma largura do painel (`531.3px` = 100%, fora do `px:5` do wrapper de texto); `panelRect.bottom - heroRect.bottom === 0` (base da imagem colada no rodapé). Console limpo. | Feedback do usuário: (1) tirar a técnica de `background-image`/`position:absolute` e voltar a imagem pro fluxo normal, abaixo dos componentes, mantendo `width:100%`; (2) a imagem estava herdando o `px:5` do conteúdo de texto — separado num wrapper interno só para logo/headline/parágrafo/diferenciais, imagem ficou irmã fora dele; (3) base da imagem deve colar no rodapé — resolvido com `margin-top: auto` na imagem dentro do painel `flex column` (empurra a imagem para o fim do espaço disponível sem esticar/distorcer). Ilustração trocada por `img_login2.png` (fornecida pelo usuário), substituindo `login-hero.png`. |
| Ajuste 2 (headline vs. logo, largura 42%) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: painel de branding mede `42.0%` da viewport; `getComputedStyle` da headline retorna `18px`, da wordmark da logo retorna `20px` (headline agora menor que a logo). Console limpo. | Feedback do usuário: "Despesas compartilhadas, contas em dia." estava maior que a logo — reduzido de `1.75rem` para `1.125rem`; painel de branding de `38%` para `42%` da largura. |
| Ajuste pós-TASK-077 (§2.1-2.3, seletor de idioma) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: painel de branding mede `38.0%` da largura da viewport (`486.39px`/`1280px`), headline em `28px` (era `34px`), input de e-mail com `46px` de altura (era ~`56px`); `read_page` confirma o selo "Seguro e confiável · Seus dados protegidos" agora dentro de `contentinfo` (rodapé global), ao lado de copyright/links; clique no seletor de idioma abre `listbox` com `pt-BR`/`en-US`, selecionar "English (US)" atualiza o `combobox` sem alterar nenhum texto da página (confirma que é só visual). Console limpo. | Feedback direto do usuário (mockup `tela_login.png`) após a TASK-077: textos menores (headline/parágrafo/diferenciais/título do card), painel de branding de 50% para 38% da largura (`flexBasis` fixo em vez de `flex:1`), inputs de 56px para 46px de altura, selo de segurança movido de `LoginBrandingPanel` para `LoginPageFooter` (agora com `justify-content: space-between`), e seletor de idioma trocado de texto estático para `Select` do MUI com `pt-BR`/`en-US` (sem lógica de tradução — registrado como backlog `016`, `docs/backlog/i18n-frontend.md`). |
| TASK-070 | Concluída | 2026-08-19 | IA (Claude) | `cd frontend && npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando (6 arquivos), sem regressão. `read_page`/`read_console_messages` no preview (`localhost:3000`) confirmam: seletor de idioma, card de login (form intacto) e rodapé com links `href="#"` renderizados, sem erro de console. | `node_modules` do frontend estava corrompido (dist truncado de `react-router-dom`) — reinstalado via `rm -rf node_modules && npm ci` antes de validar; não é regressão desta task, é o problema já registrado no backlog `005`. Screenshot visual não disponível neste ambiente (Browser pane não exibido), validação feita via árvore de acessibilidade. |
| TASK-071 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando. `read_page` confirma título "Bem-vindo de volta!", campos com placeholder/ícone, "Lembrar de mim", link "Esqueci minha senha" (`href="#"`) e botão "Entrar"; clique no botão de mostrar senha (`ref_10`) alterna `input#password` de `type="password"` para `type="text"` (`javascript_tool`). | Após o `npm ci` da TASK-070, o cache do Vite (`node_modules/.vite`) ficou dessincronizado e causou erro de "Invalid hook call" só no console (React duplicado no bundle antigo) — a página renderizava e funcionava normalmente (confirmado pelo teste de clique acima). Resolvido limpando `node_modules/.vite` e reiniciando o dev server; não é bug do código desta task. |
| TASK-072 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando. `read_page` confirma divisor "ou continue com", links "Google"/"Microsoft" e "Cadastre-se" (todos `href="#"`); `read_network_requests` após clicar em "Google" mostra só os módulos já carregados pelo Vite, nenhuma requisição nova disparada. Console limpo (0 erros) após reabrir a aba do preview. | Criados `frontend/src/assets/illustrations/{google-logo.svg,microsoft-logo.svg}` — representações geométricas simplificadas (cores oficiais, sem tracejar arte de terceiros). |
| TASK-073 | Concluída | 2026-08-19 | IA (Claude) | `python -c "import xml.dom.minidom as m; m.parse('src/assets/illustrations/login-hero.svg')"` — sem erro (XML bem formado) [versão inicial]. `GET http://localhost:3000/src/assets/illustrations/login-hero.png` via preview → `200 OK` (`read_network_requests`) [versão final]. | Criada inicialmente como SVG original (3 pessoas + notebook + badges). O usuário forneceu depois uma ilustração pronta (`E:\Projetos\Controle de Despesas\assets\images\img_login.png`) e pediu para usá-la — copiada para `frontend/src/assets/illustrations/login-hero.png`, substituindo o SVG (removido). Screenshot visual não disponível neste ambiente; validação por HTTP 200 + `read_page`/console limpo na TASK-074. |
| TASK-074 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando (1 rerun após falha transitória de concorrência com `npm ci`/`composer install` em paralelo — não relacionada ao código). `read_page` confirma logo, headline em duas linhas, parágrafo, os 3 diferenciais com título/descrição, e selo de rodapé; `read_network_requests` confirma `login-hero.png` e `expense.png` carregando com `200 OK`. Console limpo (0 erros) após reabrir a aba do preview. | Logo oficial copiado de `assets/images/expense.png` (raiz do projeto) para `frontend/src/assets/images/expense.png`. Painel com bastante conteúdo empilhado gera scroll vertical da página em viewports baixos (ex.: 1280×720 → `scrollHeight` 1023px) — comportamento aceito por ora (sem requisito de caber em uma tela), pode ser revisitado se o usuário achar ruim ao ver rodando. |
| TASK-075 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando. `git diff --stat -- frontend/src/theme.ts` — sem saída (arquivo intocado). `javascript_tool`: `getComputedStyle` do botão "Entrar" e do texto "contas em dia." retornam `rgb(31, 166, 74)` (= `#1FA64A`, `loginColors.primary`). Console limpo. | Criado `frontend/src/pages/login/colors.ts` (`loginColors`: `primary #1FA64A`, `primaryDark #168A3D`, `primaryLight #E8F5E9`, `textDark #122B4F`) aplicado via `sx` em `LoginFormCard`/`LoginBrandingPanel` (botão Entrar, checkbox, links, ícone de cadeado, headline/ícones dos diferenciais). |
| TASK-076 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 21/21 testes passando. `javascript_tool` inspeciona a stylesheet gerada e confirma as regras `@media (min-width: 0px) { .css-xyhtco { display: none; } }` e `@media (min-width: 900px) { .css-xyhtco { display: flex; } }` na classe do `LoginBrandingPanel` (breakpoint `md` padrão do MUI = 900px). | `resize_window`/`preview` não alteraram o viewport real neste ambiente (pane sem exibição física — `computer screenshot` já havia falhado antes por esse motivo); não foi possível confirmar visualmente abaixo de 900px, validação feita por inspeção direta da regra CSS gerada em vez de captura visual. |
| TASK-074 (revisão) | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 passando. `javascript_tool`: `getComputedStyle` do texto "Shared" retorna `fontFamily: "Poppins, sans-serif"`, `fontWeight: "700"`, `color: rgb(18, 43, 79)` (`loginColors.textDark`). Console limpo. | O usuário forneceu um segundo asset (`E:\Projetos\Controle de Despesas\assets\images\logo-expense.png`, só o ícone, sem texto) e pediu a fonte Google "Poppins" (700/800) para o wordmark "Shared Expense". Copiado para `frontend/src/assets/images/logo-expense.png` (substitui `expense.png`, removido); wordmark agora é texto HTML ("Shared" `textDark` + "Expense" `primary`) em vez de fazer parte da imagem — bate com o que já estava descrito em `specify.md` §2.2 desde o início. Fonte carregada via `<link>` do Google Fonts em `index.html` (preconnect + stylesheet), não `@import` em CSS (evita bloqueio de render). |
| TASK-077 | Concluída | 2026-08-19 | IA (Claude) | `npx tsc --noEmit` — exit 0. `npx vitest run` — 25/25 testes passando (4 novos em `LoginPage.test.tsx`: campos/botão renderizados; submit chama `fetch` mockado para `${API_BASE_URL}/api/login` com o payload digitado; headline + 3 diferenciais presentes; links sociais/Cadastre-se/Esqueci minha senha com `href="#"`). | Seguido o padrão de testes já usado no projeto (`@testing-library/react` + `MemoryRouter` + mock de `useNavigate`, ver `GroupForm.test.tsx`/`RequireAuth.test.tsx`); como `LoginPage` usa `fetch` global (não `axios`), mockado via `vi.stubGlobal('fetch', ...)`. |
