# Implementation — Editar Tipo de Despesa (com trava para parcelada já iniciada)

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260826

---

## 1. Desvios do fluxo padrão

Todas as tasks implementadas direto na branch da feature (`frontend/20260826-editar-tipo-despesa`), sem sub-branch por task — mesmo padrão já usado nas features anteriores desta semana.

**Desvio à parte, fora desta feature**: durante a implementação, um teste novo expôs um bug crítico já em produção (edição de despesa multiplicando `total_value` por ~100 em qualquer save, mesmo sem tocar em Valor) — corrigido numa branch separada por pedido do usuário: `docs/feature/20260826-fix-edicao-despesa-valor-corrompido/`. Os testes novos desta feature que exercitam o fluxo de salvar (`ExpenseView.test.tsx`) contornam o bug explicitamente (retypam o campo Valor antes de salvar) em vez de depender de qual das duas branches mergeia primeiro em `dev`.

## 2. Log de implementação

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-001 | Concluída | 20260826 | IA | `ExpenseController::update()`: validação ganhou `expense_type` (`in:IN_CASH,IN_INSTALLMENTS`), `installments`, `quotas`. Bloqueia 422 se a despesa é `FIXED` e o payload traz `expense_type`; bloqueia 422 a edição inteira se `IN_INSTALLMENTS` tem quota paga (antes de olhar qualquer outro campo); mantém o bloqueio existente (agora cobrindo `expense_type` além de `total_value`) pros demais casos com quota paga. | — |
| TASK-002 | Concluída | 20260826 | IA | Mesmo método: ao detectar `expense_type` no payload, calcula tipo/total_value finais, valida `installments`/soma de `quotas` (mesma checagem de `store()`) pra `IN_INSTALLMENTS`, monta 1 quota só pra `IN_CASH`; depois do `$expense->update()`, `$expense->quotas()->delete()` + recria as quotas calculadas. | — |
| TASK-003 | Concluída | 20260826 | IA | `ExpenseControllerShowUpdateDestroyTest.php`: `test_update_ignores_expense_type_installments_and_quotas` substituído por `test_update_rejects_fixed_as_a_target_type` + `test_update_rejects_changing_type_of_a_fixed_expense` + `test_update_applies_a_real_change_from_in_cash_to_in_installments`. Mais 4 testes novos: `test_update_collapses_installments_to_in_cash`, `test_update_rejects_installments_count_not_matching_quotas_count`, `test_update_rejects_quotas_sum_not_matching_total_value`, `test_update_blocks_entire_edit_when_installments_expense_has_any_quota_paid`. `php artisan test --filter=ExpenseControllerShowUpdateDestroyTest` → **25/25 verde** (confirma também que `test_update_allows_non_value_changes_when_expense_is_paid` e `test_update_rejects_total_value_change_for_installments_when_any_quota_is_paid`, pré-existentes, continuam passando sem alteração). `./vendor/bin/pint --test` nos arquivos tocados → PASS. `php artisan test` completo → **218/218 (666 assertions)**, sem regressão. | Também reaplicado aqui o fix de escopo de asserção de `SettlementConfirmationControllerTest.php` (`assertDatabaseCount`/`firstOrFail()` sem filtro → queries escopadas por grupo/par) — já corrigido e commitado em `docs/feature/20260825-pagamentos-grid-pix/`, mas essa branch nasceu de um `dev` anterior a esse commit chegar lá; reaplicado só pra manter a verificação desta branch limpa, sem crédito/task própria aqui. |
| TASK-004 | Concluída | 20260826 | IA | Criado `frontend/src/utils/installments.ts` (`pad`/`addMonthsClamped`/`buildInstallmentQuotas`, extraídos de `ExpenseForm.tsx`, código idêntico). `ExpenseForm.tsx` importa de lá. `npx tsc --noEmit` → sem erro. | — |
| TASK-005 | Concluída | 20260826 | IA | `ExpenseView.tsx`: `ExpenseDetail` ganhou `installments`; `ExpenseQuota` ganhou `number`. Novo estado `expenseType`/`installmentsCount`, pré-preenchido em `startEditing()`. Campo "Tipo de despesa" (À Vista/Parcelada) só quando `expense.expense_type !== 'FIXED'`; campo de parcelas condicional; rótulo de data dinâmico (`dateFieldLabel`). Botão "Editar" desabilitado + `Tooltip` explicando o motivo quando `isInstallmentsLocked(expense)` (`IN_INSTALLMENTS` com alguma quota paga). `handleSave()` monta `quotas` via `buildInstallmentQuotas` (Parcelada) ou 1 quota (À Vista) e inclui `expense_type`/`installments`/`quotas` no PUT quando a despesa não é `FIXED`. | — |
| TASK-006 | Concluída | 20260826 | IA | `ExpenseView.test.tsx`: 5 testes novos — tipo ausente pra despesa Fixa (fixture padrão); troca À Vista→Parcelada envia `expense_type`/`installments`/`quotas` corretos; "Editar" desabilitado com quota paga (parcelada); "Editar" habilitado sem quota paga (parcelada). `npx vitest run src/pages/ExpenseView.test.tsx src/pages/ExpenseForm.test.tsx` → **19/19 verde**. `npx tsc --noEmit` → sem erro. | — |
