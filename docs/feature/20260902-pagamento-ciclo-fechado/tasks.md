# Tasks — Pagamento em ciclo fechado

> Formato igual ao usado no SDD geral — ver `docs/sdd/03-tasks.md` para a definição completa do formato e a regra de atomicidade ("se a descrição tem 'e' ligando duas entregas independentes, é duas tasks").

Versão: 1.0 · Criado em: 20260902

| ID | Título | Tipo | Plan ref | Gate humano | Status |
|---|---|---|---|---|---|
| TASK-244 | Criar migration `settled_at` em `ex_group_cycle_snapshots` (aditiva + backfill), `GroupCycleSnapshot::isSealed()` e os helpers `cycleIsFullySettled` / `sealCycleIfSettled` / `unsealIfBroken` em `ExpenseController` | backend | plan.md §0 | antes do deploy/migration em produção | Concluída (2026-09-02) |
| TASK-245 | `pay()` / `unpay()`: aceitar `cycles_ago`, remover a trava `rejectIfCompetenceClosed`, recusar só competência `future`, chamar `sealCycleIfSettled`/`unsealIfBroken`; renomear `resolveQuotaForCurrentCompetence` → `resolveQuotaForCycle` | backend | plan.md §1 | nenhum | Concluída (2026-09-02) |
| TASK-246 | `confirmSettlement()`: aceitar `cycles_ago` e inverter a guarda — só aceita comprovante do devedor em competência fechada (`closed`/`closed_manually`), recusa `open`/`future`/selada; chamar `sealCycleIfSettled` | backend | plan.md §2 | nenhum | Concluída (2026-09-02) |
| TASK-247 | `summary()`: nova precedência de fonte de dados (selado→congelado, `closed`/`closed_manually` não selado→ao vivo com selagem preguiçosa), campo `cycle.settled`; `cycleHistory()` só ciclos com `settled_at`; `reopen()` recusa ciclo selado; `close()`/`grossDebts()` devolvem `settled`; remover `cycleSnapshotFor()` | backend | plan.md §3 | nenhum | Concluída (2026-09-02) |
| TASK-248 | Rota e método `GET /api/groups/{groupId}/expenses/focus-cycle` — devolve `{ cycles_ago }` do ciclo fechado pendente mais recente (teto 12), ou 0 | backend | plan.md §4 | nenhum | Concluída (2026-09-02) |
| TASK-249 | Ordenar não-pago-primeiro: `computeCycleSummary()` por `[paid asc, date asc]`; `attachSettlementConfirmations()` por `[confirmado asc, amount desc]` | backend | plan.md §7 | nenhum | Concluída (2026-09-02) |
| TASK-250 | `useGroupCycle.ts`: buscar `focus-cycle` ao trocar de grupo e semear `cyclesAgo` com o retorno (estado `focusResolved` barrando o fetch de summary); `SummaryCycle` ganha `settled: boolean` | frontend | plan.md §4 | nenhum | Concluída (2026-09-02) |
| TASK-251 | Front do acerto: `PayableSettlementList` ganha prop `canConfirm` (esconde os botões do devedor + mostra aviso quando ciclo não fechado/selado); `Payments.tsx` calcula e passa | frontend | plan.md §2 | nenhum | Concluída (2026-09-02) |
| TASK-252 | Front do pagamento: `usePaymentActions` recebe/envia `cycles_ago` e recalcula `canPay`/`canUnpay` contra `settled`/`future` (não mais só `open`); `ExpenseManager` renomeia `cycleIsOpen`→`cycleIsEditable` para as ações de edição; `Payments` e `ExpenseManager` passam `cyclesAgo` ao hook | frontend | plan.md §1 | nenhum | Concluída (2026-09-02) |
| TASK-253 | Menu: `Pagamentos` antes de `Participantes` em `groupNavItems` e `simpleNavItems`; ajustar as assertivas de ordem em `GroupShellLayout.test.tsx` e `SimpleShellLayout.test.tsx` | frontend | plan.md §5 | nenhum | Pendente |
| TASK-254 | Menu sem grupo: criar `MembersEntry.tsx` e `ReportsEntry.tsx` (cópias de `PaymentsEntry`), rotas `/members` e `/reports` em `App.tsx`, `to:` nos itens `Participantes`/`Relatórios` de `simpleNavItems`; ajustar `SimpleShellLayout.test.tsx` | frontend | plan.md §6 | nenhum | Pendente |
| TASK-255 | Adicionar ponteiro de supersessão parcial no `specify.md` de `docs/feature/20260821-home-ciclo-corrente-navegacao-futura/` (o default de ciclo desta feature substitui o daquela quando há ciclo fechado pendente) | doc | plan.md §4 | nenhum | Pendente |
| TASK-256 | `BillingCycle`: `GRACE_DAYS = 5`, `closesAt()` público, `statusOf()` fecha em `end + GRACE_DAYS`; campo aditivo `cycle.closes_at` em `summary()`/`close()`/`reopen()`/`grossDebts()`; reescrever/ampliar `BillingCycleTest` | backend | plan.md §9 | antes do deploy/migration em produção | Concluída (2026-09-02) |
| TASK-257 | `focusCycle()`: focar também ciclo em carência (`open` com `end` já passado) com pendência; caso novo em `FocusCycleTest` | backend | plan.md §10 | nenhum | Concluída (2026-09-02) |
| TASK-258 | Ajustar relógio/datas dos Feature tests que assumem "fecha na virada" (`ExpenseControllerCloseTest`, `ExpenseControllerReopenTest`, `ExpenseControllerCycleFreezeTest`, `ExpenseControllerCycleHistoryTest`, `GroupCycleSnapshotTest`, `ExpenseControllerPayTest`) para a janela de carência | backend | plan.md §9 | nenhum | Concluída — no-op (2026-09-02) |
| TASK-259 | `CycleClosingAlert.tsx` (pré/pós derivados do `summary`) + render em `GroupSummary.tsx` e `ExpenseManager.tsx` + `SummaryCycle.closes_at` no hook + `CycleClosingAlert.test.tsx` | frontend | plan.md §11 | nenhum | Pendente |

**Ordem de execução:** TASK-244 primeiro (base de tudo). Depois TASK-245 → 246 → 247 → 248 → 249 em série (editam o mesmo `ExpenseController.php`). TASK-247 antes de TASK-250 e TASK-251; TASK-248 antes de TASK-250 (leem `cycle.settled` / o endpoint). Frontend: TASK-250 → 252 → 251. TASK-253 e TASK-254 são independentes de todo o resto — podem entrar a qualquer momento. TASK-255 por último. Cada task inclui seus testes (PHPUnit / Vitest) no próprio escopo — nenhuma task é "pronta" com teste vermelho (`00-constitution.md` §2.2/§2.4). Antes do PR único contra `dev`: rodar o agent `security-reviewer` (TASK-245/246/247 mexem em `pay`/`unpay`/`confirmSettlement`; TASK-256/257 mexem em `summary`/`grossDebts`/`focusCycle`) e o `pr-readiness-checker`.

**Carência + banners (TASK-256..259, plan.md §9–§11):** TASK-256 no bloco backend depois de TASK-244, serializada com 245–248 (mesmo `ExpenseController`); TASK-258 logo após TASK-256 (mantém `php artisan test` verde). TASK-257 depois de TASK-248. TASK-259 (frontend) depois de TASK-256 **e** TASK-250. O ponteiro em `docs/feature/20260902-fechamento-ciclo-carencia-dia-5/` já foi criado nesta rodada (não vira task).

## Critérios de aceite

- **TASK-244**: `php artisan migrate` cria a coluna `ex_group_cycle_snapshots.settled_at` (nullable); um teste de migration/feature mostra que toda linha pré-existente ficou com `settled_at = updated_at` e que uma linha nova nasce com `settled_at = null`. Teste unitário de `cycleIsFullySettled()`: retorna `false` com `totals.pending > 0`; `false` com um par de `settlements` sem `SettlementConfirmation`; `true` quando ambos batem; `true` para ciclo sem entradas. `GroupCycleSnapshot::isSealed()` reflete `settled_at`.
- **TASK-245**: teste de feature — credor faz `POST /api/expenses/{id}/pay` com `cycles_ago` de um ciclo já `closed` por data → 200 e a `Quota` da competência-alvo fica `paid=true`; `cycles_ago` de competência `future` → 422; o teste hoje chamado `test_cannot_pay_in_a_manually_closed_cycle` passa a afirmar 200. `unpay` análogo, e um `unpay` que quebra a quitação total limpa `settled_at` do snapshot.
- **TASK-246**: teste de feature — `POST /api/groups/{id}/settlements/confirm` com a competência `open` (sem fechamento manual) → 422 com a mensagem de "após o fechamento"; com `closed_manually` ativo → 200; com ciclo `closed` por data (via `cycles_ago`) → 200; ciclo selado → 422. O teste hoje chamado `test_cannot_confirm_in_a_manually_closed_cycle` passa a afirmar 200.
- **TASK-247**: teste de feature — `GET /api/groups/{id}/expenses/summary?cycles_ago=1` de um ciclo `closed` não quitado devolve dados **ao vivo** (marca uma quota via `pay`, o summary seguinte já reflete) e `cycle.settled=false`; ao quitar contas + acertos, o summary passa a `cycle.settled=true` e o conteúdo igual ao snapshot persistido; `GET /api/groups/{id}/expenses/cycles` lista só ciclos com `settled_at`; `POST /api/groups/{id}/expenses/reopen` em ciclo selado → 422. `cycleSnapshotFor()` não existe mais (grep vazio).
- **TASK-248**: teste `FocusCycleTest` — com um ciclo fechado pendente em `cycles_ago=2` e o `=1` quitado, `GET /api/groups/{id}/expenses/focus-cycle` → `{"cycles_ago":2}`; com tudo quitado → `{"cycles_ago":0}`; ciclo selado nunca é retornado.
- **TASK-249**: `ExpenseControllerSummaryTest` afirma que no array `expenses` da resposta toda entrada com `paid=false` vem antes de qualquer `paid=true`, e dentro de cada bloco a ordem é por data; no array `settlements`, todo item sem `confirmedAt` vem antes dos confirmados. Testes de ordem existentes atualizados e verdes (`php artisan test`).
- **TASK-250**: teste de `useGroupCycle` (Vitest) com `axios` mockado — ao montar com um `groupId`, chama `GET …/expenses/focus-cycle` e o primeiro `GET …/summary` sai com `cycles_ago` igual ao retornado; nenhum `GET …/summary` dispara antes de `focus-cycle` resolver. `npx tsc --noEmit` limpo.
- **TASK-251**: teste de `PayableSettlementList` — com `canConfirm={false}` e usuário = devedor, os botões "Pagar com Pix"/"Enviar comprovante" não estão no DOM e há um texto de aviso; com `canConfirm={true}` os botões aparecem.
- **TASK-252**: teste de `usePaymentActions` — `handlePay`/`handleUnpay` incluem `cycles_ago` no corpo do POST; `canPay(exp)` é `true` para o credor num ciclo `closed` não selado e `false` quando `summary.cycle.status === 'future'`. `ExpenseManager.tsx` e `Payments.tsx` compilam (`tsc --noEmit`).
- **TASK-253**: `GroupShellLayout.test.tsx` e `SimpleShellLayout.test.tsx` afirmam a ordem dos itens do menu como Home → Despesas → Pagamentos → Participantes → Relatórios → … (`npm test` verde).
- **TASK-254**: `SimpleShellLayout.test.tsx` afirma `href="/members"` para Participantes e `href="/reports"` para Relatórios (não `"#"`); um teste de rota cobre que acessar `/members` sem grupo redireciona para `/groups/:id/members` do grupo mais ativo (mesmo padrão do teste de `/payments`).
- **TASK-255**: `docs/feature/20260821-home-ciclo-corrente-navegacao-futura/specify.md` passa a conter uma nota apontando `docs/feature/20260902-pagamento-ciclo-fechado/` como supersessão parcial da regra de "abrir sempre no ciclo corrente".
- **TASK-256**: `php artisan test --filter=BillingCycleTest` verde. Casos novos/reescritos — `closing_day=null`: o ciclo de janeiro (`cycleFor(null, ref, 1)`) é `open` consultado em `2026-02-04` e `closed` em `2026-02-05`; `closing_day=10`: o ciclo `[2025-12-11, 2026-01-10]` (`cycleFor(10, ref, 1)`) é `open` em `2026-01-14` e `closed` em `2026-01-16`, e `cycleFor(10, '2026-01-14', 0)` continua `[2026-01-11, 2026-02-10]`; `BillingCycle::closesAt(Carbon::parse('2026-01-31'))->toDateString() === '2026-02-05'`. Teste de feature: `GET /api/groups/{id}/expenses/summary` (e `close`/`reopen`/`grossDebts`) devolvem `cycle.closes_at` = `cycle.end` + 5 dias no formato `Y-m-d`.
- **TASK-257**: `FocusCycleTest` — `Carbon::setTestNow('2026-02-02')`, grupo `closing_day=null` com uma quota não paga no ciclo de janeiro → `GET /api/groups/{id}/expenses/focus-cycle` → `{"cycles_ago":1}`; mesmo relógio com o ciclo de janeiro quitado → `{"cycles_ago":0}`; o ciclo corrente (fevereiro) nunca sai como pendente.
- **TASK-258**: `php artisan test` verde na suíte inteira. As únicas alterações são datas de `Carbon::setTestNow(...)` / `date_payment` — nenhuma asserção de negócio muda, salvo os testes cujo nome/intenção era "fecha na virada", que passam a exercitar `end + 5`.
- **TASK-259**: teste de `CycleClosingAlert` (Vitest) com os 4 casos do plan.md §11 (pré dentro da janela → texto "fecha em"; `closed` não quitado com 2 devedores → os 2 nomes; `settled` → `null`; `open` com `end` futuro → `null`). `npx tsc --noEmit` limpo. `GroupSummary` e `ExpenseManager` renderizam `<CycleClosingAlert summary={summary} />` (assert leve no DOM da página).
