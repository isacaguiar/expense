# Implementation — Melhoria da Gestão de Despesas do Grupo

> Como as tasks de `tasks.md` viram código. O fluxo (branch, checklist pré-PR, gates) é o de `docs/sdd/04-implementation.md` — só documente aqui um desvio específico desta feature, se houver.

Versão: 1.0 · Criado em: 20260821

---

## 1. Desvios do fluxo padrão (se houver)

<Deixe vazio/apague esta seção se a feature segue `04-implementation.md` sem exceção.>

## 2. Log de implementação

Preenchido conforme as tasks de `tasks.md` são executadas. Uma linha por task. Cite o comando real executado e o resultado obtido — não basta escrever "testado"/"validado" em prosa.

| Task ID | Status | Data | Responsável | Comandos executados / resultado | Observações |
|---|---|---|---|---|---|
| TASK-149 | Implementada | 2026-08-22 | IA (Claude Code) | `php artisan migrate --path=database/migrations/2026_08_22_120000_add_paid_at_and_paid_by_to_ex_quotas_table.php` — rodou limpo; `php artisan tinker` confirmou colunas `paid_at`/`paid_by` em `ex_quotas`; `./vendor/bin/pint --test app/Models/Quota.php database/migrations/2026_08_22_120000_...php` — PASS (2 files); `php artisan test` (suíte completa) — 114 passed (311 assertions) | Migration aditiva local (coluna `paid_at` timestamp nullable, `paid_by` FK nullable para `ex_users`); model `Quota` ganhou `paid_at`/`paid_by` em `$fillable`/`$casts` e relação `paidBy()` |
| TASK-150 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php` — PASS (1 file); `php artisan test` (suíte completa) — 114 passed (311 assertions), sem regressão | `ExpenseController::store` — removida a regra de validação `quotas.*.paid` (cliente não decide mais o status inicial) e o `paid` de cada `Quota` criada passa a ser hardcoded `false`, ignorando qualquer valor enviado no payload |
| TASK-151 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerStoreTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerStoreTest` — 14 passed (32 assertions); `php artisan test` (suíte completa) — 117 passed (319 assertions) | 3 testes novos em `ExpenseControllerStoreTest.php` cobrindo `IN_CASH`, `IN_INSTALLMENTS` (2 quotas) e `FIXED`, todos enviando `paid: true` no payload e confirmando `paid = false` no banco |
| TASK-152 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerMaterializeFixedOccurrenceQuotaTest.php` — PASS (2 files, 1 fix automático de estilo aplicado no teste); `php artisan test --filter=ExpenseControllerMaterializeFixedOccurrenceQuotaTest` — 3 passed (5 assertions); `php artisan test` (suíte completa) — 120 passed (324 assertions) | Novo método privado `ExpenseController::materializeFixedOccurrenceQuota()`; ainda não é chamado por nenhum fluxo de produção (isso é a TASK-153) — testado isoladamente via `ReflectionMethod` em `ExpenseControllerMaterializeFixedOccurrenceQuotaTest.php` (busca/cria sem duplicar, valor congela após `total_value` mudar) |
| TASK-153 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerSummaryTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerSummaryTest` — 9 passed (48 assertions); `php artisan test` (suíte completa) — 121 passed (330 assertions) | `collectCycleEntries` — ocorrência de `FIXED` com `Quota` já materializada usa `value_quota`/`paid` dela (mesmo padrão de `$direct`); sem `Quota`, continua projetando ao vivo do `total_value` (comportamento preservado, coberto pelo teste já existente `fixed_expense_projected_without_own_quota_defaults_to_pending`). Teste novo `fixed_expense_uses_materialized_quota_value_when_present` prova que o resumo usa o valor congelado (350) em vez do `total_value` ao vivo (500) |
| TASK-154 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerCycleFreezeTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerCycleFreezeTest` — 8 passed (33 assertions); `php artisan test` (suíte completa) — 122 passed (336 assertions) | Teste novo `fixed_expense_value_edited_later_does_not_change_a_month_whose_quota_was_already_materialized`: materializa a `Quota` de julho (350), edita `total_value` para 999, e só então lê o resumo de julho pela primeira vez (que dispara a criação do `GroupCycleSnapshot`) — confirma `value: 350`, reproduzindo e fechando exatamente o gap original do `specify.md` §2.3 (snapshot lazy nunca lido antes da edição) |
| TASK-155 | Implementada | 2026-08-22 | IA (Claude Code) | `php artisan migrate --path=database/migrations/2026_08_22_130000_...php` — rodou limpo; `./vendor/bin/pint --test app/Models/GroupCycleSnapshot.php database/migrations/2026_08_22_130000_...php tests/Feature/GroupCycleSnapshotTest.php` — PASS (3 files); `php artisan test --filter=GroupCycleSnapshotTest` — 5 passed (5 assertions); `php artisan test` (suíte completa) — 127 passed (341 assertions) | Migration aditiva local (`closed_manually_at`/`reopened_at`, timestamps nullable); model ganhou os campos em `$fillable`/`$casts` e o helper `isManuallyClosedAndActive()` (fechado manualmente e sem reabertura mais recente que o fechamento) — testado isolado com os 4 estados possíveis + regressão do `unique(group_id, cycle_start)` |
| TASK-156 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerCycleFreezeTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerCycleFreezeTest` — 11 passed (39 assertions); `php artisan test` (suíte completa) — 130 passed (347 assertions) | Novo helper privado `rejectIfCompetenceClosed(Group, Carbon)` — considera fechamento automático (`BillingCycle::statusFor`) OU manual (`GroupCycleSnapshot::isManuallyClosedAndActive`); `rejectIfCycleClosed(Expense)` vira wrapper fino dele (mantém o bypass de `FIXED`). 3 testes novos provam: `update`/`destroy` de `IN_CASH` bloqueados numa competência fechada manualmente mesmo com `BillingCycle` ainda `open`, e liberados de novo após reabertura simulada |
| TASK-157 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php routes/api.php tests/Feature/ExpenseControllerCloseTest.php` — PASS (3 files); `php artisan test --filter=ExpenseControllerCloseTest` — 4 passed (18 assertions); `php artisan test` (suíte completa) — 134 passed (365 assertions) | Nova rota `POST /groups/{groupId}/expenses/close` + `ExpenseController::close`. Achado durante a implementação: o guard planejado ("só permitido se a competência vigente estiver `open`") era código morto — `close()` sempre opera sobre o ciclo que contém "agora", que por construção do `BillingCycle::cycleFor(cyclesAgo=0)` está sempre `open` em relação a "agora"; removido em vez de mantido sem uso (`tasks.md` do TASK-157 corrigido de acordo). Upsert via `GroupCycleSnapshot::updateOrCreate` confirmado sem duplicar em 2 fechamentos seguidos |
| TASK-158 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerCloseTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerCloseTest` — 6 passed (26 assertions); `php artisan test` (suíte completa) — 136 passed (373 assertions) | `summary()` passa a checar `GroupCycleSnapshot::isManuallyClosedAndActive()` quando `BillingCycle` diz `open`, devolvendo `cycle.status: 'closed_manually'` e os dados da foto (não recomputa ao vivo) nesse caso. 2 testes novos: resumo reflete a foto (não edições ao vivo) enquanto fechado manualmente; volta a `'open'` e a refletir edições ao vivo depois de `reopened_at` ser setado |
| TASK-159 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerStoreTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerStoreTest` — 17 passed (38 assertions); `php artisan test` (suíte completa) — 139 passed (379 assertions) | `store()` agora chama `rejectIfCompetenceClosed($group, date_payment)` para todo `expense_type` (inclusive `FIXED`, ao contrário de `update()`/`destroy()` — criar uma despesa nova no passado não tem a mesma justificativa de "definição recorrente"). 3 testes novos: rejeita `IN_CASH` e `FIXED` com `date_payment` em ciclo automaticamente fechado, e rejeita em competência fechada manualmente |
| TASK-160 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerCloseTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerCloseTest` — 7 passed (36 assertions); `php artisan test` (suíte completa) — 140 passed (389 assertions) | Os cenários individuais de fechar/bloquear já tinham teste dedicado nas TASK-156/157/159 (criados simulando o estado do snapshot direto no banco, já que `close()` não existia ainda em algumas delas). Esta task adiciona 1 teste de integração ponta a ponta novo: fecha via a rota real `POST .../close`, confirma `update`/`destroy`/`store` bloqueados (422) na mesma competência, e que re-fechar não duplica o snapshot — cobre a integração entre as tasks que o checklist individual de cada uma não pegaria |
| TASK-161 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php routes/api.php tests/Feature/ExpenseControllerReopenTest.php` — PASS (3 files); `php artisan test --filter=ExpenseControllerReopenTest` — 5 passed (19 assertions); `php artisan test` (suíte completa) — 145 passed (408 assertions) | Nova rota `POST /groups/{groupId}/expenses/reopen` + `ExpenseController::reopen`. Só opera sobre a competência que contém "agora" (mesmo desenho de `close()`) — a regra "não reabre depois da virada do mês" sai de graça dessa restrição, sem checagem extra de `BillingCycle`: depois que "agora" vira o mês seguinte, o snapshot antigo simplesmente não é mais encontrado pela consulta (`cycle_start` = início do mês atual). 5 testes novos: reabre e volta a computar ao vivo, `update` desbloqueado depois, falha sem fechamento manual ativo, falha se já foi reaberto antes, exige membership |
| TASK-162 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerReopenTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerReopenTest` — 6 passed (26 assertions); `php artisan test` (suíte completa) — 146 passed (415 assertions) | Teste novo `reopen_fails_after_the_month_turns_over_even_with_manual_closure_still_present`: fecha agosto manualmente, avança `Carbon::setTestNow` para setembro, confirma `reopen` 422 e o snapshot de agosto permanece intocado (`reopened_at` continua `null`) — e que `GET .../summary?cycles_ago=1` passa a tratá-lo como `closed` automático, definitivo. Achado ao escrever o teste: o JWT expira em relação ao relógio virtual — precisou gerar um token novo depois de avançar `Carbon::setTestNow`, senão a chamada falha com 401 antes de chegar na regra de negócio |
| TASK-163 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php routes/api.php tests/Feature/ExpenseControllerPayTest.php` — PASS (3 files); `php artisan test --filter=ExpenseControllerPayTest` — 6 passed (16 assertions); `php artisan test` (suíte completa) — 152 passed (431 assertions) | Nova rota `POST /expenses/{expenseId}/pay` + `ExpenseController::pay`, e novo helper privado `resolveQuotaForCurrentCompetence(Expense, start, end)` (reaproveitado por `unpay` na TASK-164) — materializa `Quota` de `FIXED` via `collectCycleEntries`/`materializeFixedOccurrenceQuota`, ou busca a `Quota` já existente por `date_expected` pra `IN_CASH`/`IN_INSTALLMENTS`. Só o credor (`user_payer_id`) pode pagar; sempre opera sobre a competência vigente (mesmo desenho de `close`/`reopen`). 6 testes novos: paga `IN_CASH`, materializa e paga `FIXED`, 403 pra não-credor, 422 sem ocorrência na competência vigente, 422 em competência fechada manualmente, 404 sem membership |
| TASK-164 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php routes/api.php tests/Feature/ExpenseControllerPayTest.php` — PASS (3 files); `php artisan test --filter=ExpenseControllerPayTest` — 12 passed (29 assertions); `php artisan test` (suíte completa) — 158 passed (444 assertions) | Nova rota `POST /expenses/{expenseId}/unpay` + `ExpenseController::unpay`. Decisão deliberada: **não** reaproveita `resolveQuotaForCurrentCompetence` (que materializa `FIXED` sob demanda) — busca a `Quota` só se já existir, porque uma ocorrência virtual nunca está paga, e materializar à toa numa tentativa de desfazer congelaria o valor do mês vigente sem necessidade. 6 testes novos, incluindo `unpay_does_not_materialize_a_fixed_quota_that_was_never_paid` confirmando esse cuidado |
| TASK-165 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerShowUpdateDestroyTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerShowUpdateDestroyTest` — 17 passed (33 assertions); `php artisan test` (suíte completa) — 162 passed (452 assertions) | `update()` bloqueia mudança de `total_value` quando qualquer `Quota` da despesa está `paid=true` (422), mas continua permitindo mudar outros campos (descrição, etc.). Decisão: `FIXED` fica de fora dessa checagem (mesmo raciocínio de `rejectIfCycleClosed`) — seu `total_value` é o valor do template pra ocorrências futuras, e uma ocorrência já paga tem `Quota` própria congelada que essa edição não alcança. 4 testes novos: bloqueia valor se pago, permite outros campos se pago, bloqueia `IN_INSTALLMENTS` se qualquer parcela estiver paga, permite `FIXED` mesmo com ocorrência passada paga |
| TASK-166 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerPayTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerPayTest` — 13 passed (38 assertions); `php artisan test` (suíte completa) — 163 passed (461 assertions) | Os cenários individuais de autorização/competência de pay/unpay e o bloqueio de valor já tinham teste dedicado nas TASK-163/164/165. Esta task adiciona 1 teste de integração ponta a ponta: edita valor (pendente, funciona) → paga → edita valor (bloqueado) → edita descrição (funciona) → despaga → edita valor de novo (funciona) — confirma o ciclo completo TASK-163+164+165 |
| TASK-167 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerShowUpdateDestroyTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerShowUpdateDestroyTest` — 19 passed (37 assertions); `php artisan test` (suíte completa) — 165 passed (465 assertions) | `destroy()` bloqueia exclusão (422) se qualquer `Quota` da despesa está `paid=true`. Decisão consciente: **ao contrário** de `update()` (TASK-165), aqui **não** há bypass de `FIXED` — excluir apaga a definição inteira (todas as ocorrências, inclusive pagas), não só o valor-template pra frente, então a proteção precisa valer pra todos os tipos. 2 testes novos: bloqueia `IN_CASH` pago, bloqueia `FIXED` com qualquer ocorrência paga |
| TASK-168 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerPayTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerPayTest` — 15 passed (46 assertions); `php artisan test` (suíte completa) — 167 passed (473 assertions) | 2 testes de integração novos: ciclo completo paga → excluir bloqueado (422) → despaga → excluir funciona (200); e despesa paga **e** de competência automaticamente fechada ao mesmo tempo — confirma que qualquer um dos dois motivos já basta pra recusar, sem regressão do bloqueio de competência fechada já existente |
| TASK-169 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test tests/Feature/ExpenseControllerSummaryTest.php` — PASS (1 file); `php artisan test --filter=ExpenseControllerSummaryTest` — 10 passed (55 assertions); `php artisan test` (suíte completa) — 168 passed (480 assertions) | Teste novo `balances_are_unchanged_after_the_fixed_occurrence_quota_is_materialized`: compara `balances` da mesma competência antes e depois de materializar a `Quota` do mês corrente de uma `FIXED` — `assertSame` confirma resultado idêntico byte a byte, só a origem do dado muda (projeção ao vivo → `Quota` real), confirmando que `computeCycleSummary` não depende de como o valor chegou até ele |
| TASK-170 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/GroupSummary.test.tsx` — 10 passed; `npx vitest run` (suíte completa do frontend) — 88 passed (18 arquivos), sem regressão | Novo `frontend/src/hooks/useGroupCycle.ts` (fetch de `GET .../expenses/summary`, estado de `cyclesAgo`, `reload()`) extraído de `GroupSummary.tsx`, que passou a consumi-lo sem mudança de comportamento. Achado corrigido de passagem (causado pela própria TASK-158 desta feature, não débito pré-existente): `CycleStatus`/`cycleStatusChip` não tinham entrada para `'closed_manually'` — o `Chip` quebraria (props `undefined`) assim que um grupo tivesse uma competência fechada manualmente; adicionado no tipo extraído (`label: 'Fechado (revisável)'`, `color: 'warning'`) |
| TASK-171 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/GroupSummary.test.tsx` — 10 passed; `npx vitest run` (suíte completa) — 88 passed, sem regressão | Novo `frontend/src/components/BalanceCards.tsx`. Decisão: em vez de extrair a lista (`Paper`/`List`/`ListItem`) tal como estava, foi redesenhado como card individual por participante (`MUI Card`), seguindo a referência visual de `specify.md` §2.1 (`assets/images/tela-despesas.png`) e o próprio nome do componente — o conteúdo textual (nome, "R$ X a receber/a pagar") ficou idêntico, por isso os testes existentes (que checam só por texto) continuam verdes sem alteração. Verificação visual em navegador fica para as tasks seguintes, quando `ExpenseManager.tsx` (destino real da feature) estiver consumindo o componente |
| TASK-172 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 11 passed (3 novos de navegação por competência); `npx vitest run` (suíte completa) — 91 passed (18 arquivos), sem regressão; verificação manual no navegador (usuário/grupo/despesa `FIXED` criados via `tinker`, JWT injetado em `localStorage`, dados removidos ao final) — competência anterior (julho) vazia, competência atual (agosto) com a despesa, competência futura (setembro) já projetando a ocorrência da `FIXED` | `ExpenseManager.tsx` substitui `currentDate`/`changeMonth`/`GET .../expenses?year&month` por `useGroupCycle`/`GET .../expenses/summary`; `stopFixedRecurrence` passa a derivar `year`/`month` de `summary.cycle.start` em vez de `new Date()`. Suíte de teste reescrita pra mockar a nova forma de resposta (`{cycle, totals, expenses, balances}` em vez de array puro) — corrigido também um detalhe de locale (`toLocaleDateString('pt-BR', {day:'2-digit', month:'short'})` produz "01 de ago.", não "01 ago", achado só ao rodar o teste real) |
| TASK-173 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 11 passed; `npx vitest run` (suíte completa) — 91 passed, sem regressão; verificação manual no navegador (grupo com 2 participantes, despesa `FIXED` + `IN_CASH`) — conteúdo renderizado corretamente (2 cards de despesa + 2 `BalanceCards` com saldo líquido correto, R$ 600 a receber/a pagar); em 1280px, medição de posição via `getBoundingClientRect()` confirmou "Saldo por pessoa" (x≈955) e o campo de busca (x≈318) na mesma linha (y≈220-229) — colunas lado a lado, não empilhadas | `ExpenseManager.tsx` envolve listagem+filtros (`size={{xs:12, md:8}}`) e `<BalanceCards />` (`size={{xs:12, md:4}}`) num `Grid container`, mesmo padrão já usado em `GroupSummary.tsx`. **Limitação registrada**: não foi possível confirmar visualmente o empilhamento em mobile nesta sessão — `resize_window` e `computer{screenshot}` não tiveram efeito observável (painel do navegador não compositando frames neste ambiente); a garantia de empilhamento em `xs` vem da mesma mecânica de breakpoint do MUI Grid v2 já usada (e implicitamente validada) em `GroupSummary.tsx`, não de um teste visual novo — verificação em mobile fica pendente para quando o painel do navegador estiver disponível |
| TASK-179 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseView.test.tsx` — 6 passed (2 existentes reescritos + 4 novos de edição); `npx vitest run` (suíte completa) — 94 passed, sem regressão; verificação manual no navegador (usuário/grupo/despesa criados via `tinker`) — `GET /api/expenses/{id}` confirmado funcionando (rede: 200 OK), tela renderiza descrição/valor/tipo/credor/data corretamente a partir da despesa real. Clique no botão "Editar" não pôde ser confirmado interativamente nesta sessão (`computer{left_click}` deu timeout, painel sem display) — fluxo de edição/erro coberto pelos 4 testes automatizados novos em vez disso | Task nova, não prevista no `tasks.md` original — decisão tomada com o usuário durante a TASK-174 (ver `plan.md` §1). `ExpenseView.tsx` passa a buscar `GET /expenses/{expenseId}` (recurso único, correto p/ qualquer competência) em vez da lista do mês calendário atual (quebrada fora do mês corrente). Ganhou modo de edição: descrição/valor/data/credor/pagadores via `PUT /expenses/{id}`, erro de negócio (422) exibido tal como a API devolve, sem validação client-side de regra de domínio |
| TASK-180 | Implementada | 2026-08-22 | IA (Claude Code) | `./vendor/bin/pint --test app/Http/Controllers/ExpenseController.php tests/Feature/ExpenseControllerSummaryTest.php` — PASS (2 files); `php artisan test --filter=ExpenseControllerSummaryTest` — 11 passed (59 assertions); `php artisan test` (suíte completa) — 169 passed (484 assertions) | Task nova, achado durante a TASK-174 (ver `plan.md` §1): `summary()` só devolvia nomes, sem IDs. `computeCycleSummary` passa a incluir `userPayerId`/`userCreatorId` (mudança aditiva) — teste novo confirma os dois IDs corretos numa despesa com credor e criador diferentes |
| TASK-174 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 17 passed (6 novos); `npx vitest run` (suíte completa) — 100 passed, sem regressão; verificação manual no navegador (3 despesas: `FIXED` pendente/credor eu, `IN_CASH` pendente/credor eu, `IN_CASH` paga/credor outra pessoa) — árvore de acessibilidade confirmou exatamente as combinações esperadas de ícones por despesa | Card ganha chip de Status (Paga/Pendente), rótulo "Credor" (era "Pago por"), lista de Pagadores com `Tooltip`. Ícones de ação com `Tooltip`: Editar (funcional, navega pra `ExpenseView.tsx` — TASK-179), Excluir e Marcar como paga (visibilidade condicional já implementada, `onClick` ainda placeholder — comentário no código apontando TASK-175/177, que conectam o comportamento). Regras de visibilidade: todas as ações exigem `cycle.status === 'open'`; Editar exige ser criador ou credor (`isOwner`); Excluir exige ser dono, despesa variável (`!isFixed`) e pendente; Marcar como paga exige ser credor (`isCreditor`) e pendente. Novo fetch de `GET /api/me` pra saber o usuário logado |
| TASK-175 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 21 passed (4 novos); `npx vitest run` (suíte completa) — 104 passed, sem regressão; verificação híbrida: clique interativo no ícone não pôde ser confirmado nesta sessão (mesma limitação de painel sem display já registrada na TASK-179), então o `DELETE /api/expenses/{id}` real foi chamado direto via `curl` com o token da sessão de preview — confirmado 200 + `GET .../summary` seguinte já sem a despesa, provando que a chamada que o botão dispara funciona ponta a ponta contra o backend real | Ícone "Excluir despesa" (já visível desde a TASK-174) abre `Dialog` de confirmação mostrando a descrição da despesa; confirmar chama `DELETE /expenses/{id}` e recarrega a lista (`reload()`); erro de negócio da API (422 — despesa paga, competência fechada) é exibido dentro do próprio diálogo, que permanece aberto pra o usuário tentar de novo ou cancelar |
| TASK-176 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 27 passed (6 novos); `npx vitest run` (suíte completa) — 110 passed, sem regressão; clique interativo não pôde ser confirmado nesta sessão (painel sem display) — `POST .../close` e `.../reopen` chamados via `curl` contra o backend real: fechar muda `cycle.status` de `open` pra `closed_manually`, reabrir volta pra `open`, confirmado no `GET .../summary` seguinte a cada chamada | Botões "Fechar mês"/"Reabrir mês" só aparecem quando `cyclesAgo === 0` (a competência vigente — `close()`/`reopen()` sempre operam sobre "agora" no backend, nunca sobre a competência navegada pelas setas, então mostrar o botão fora disso enganaria o usuário) **e** o status bate (`open` pra fechar, `closed_manually` pra reabrir); nenhum dos dois aparece pra `closed` automático ou `future`. 6 testes novos, incluindo um que navega pra competência anterior e confirma que os botões somem |
| TASK-177 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 31 passed (4 novos); `npx vitest run` (suíte completa) — 114 passed, sem regressão; clique interativo não pôde ser confirmado nesta sessão (painel sem display) — `POST .../pay` e `.../unpay` chamados via `curl` contra o backend real: pagar muda `paid:false→true` e `totals.paid`/`totals.pending`, desfazer reverte tudo, confirmado no `GET .../summary` seguinte a cada chamada | Ícone "Marcar como paga" (visível desde a TASK-174) conectado a `POST .../pay`; novo ícone "Desfazer pagamento" (`UndoIcon`) visível quando `cycleIsOpen && exp.paid && isCreditor(exp)` — decisão confirmada no `specify.md` §2.6 (desfazer é permitido, só pelo credor, só com competência aberta). Erro de negócio da API exibido num `Snackbar` (não bloqueia a tela, ação rápida por card, não modal como excluir) |
| TASK-178 | Implementada | 2026-08-22 | IA (Claude Code) | `npx tsc --noEmit` — sem erros; `npx vitest run src/pages/ExpenseManager.test.tsx` — 32 passed (1 novo); `npx vitest run` (suíte completa) — 115 passed, sem regressão | Os cenários individuais (grid, visibilidade condicional, exclusão, fechar/reabrir, pagar/despagar) já tinham teste dedicado nas TASK-172–177. Esta task adiciona 1 teste de integração ponta a ponta, numa única montagem contínua: renderiza grid com despesa + saldo por pessoa → marca como paga (ícones somem/aparecem corretamente) → desfaz pagamento (ícones voltam) → exclui a despesa → lista fica vazia. Cobre a transição de estado entre ações que testes isolados (cada um com sua própria montagem) não pegariam |
